the fix needs _perf/baserun.tl, which exists only on PR #1480's branch and not on main (verified with git ls-tree against origin/main and the branch). Building this first would mean either stacking on an unmerged branch, which the skill forbids, or duplicating the helper. The dependency is also evidential: this item's Evidence rests on the resolution behaviour established under 3IVF3HbV, and that technique is not settled — its predecessor PR #1463 was rejected for a manifest that silently disabled the release perf gate, and the replacement #1480 is in check, unreviewed.