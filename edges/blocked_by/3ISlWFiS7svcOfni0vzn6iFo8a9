the other half of the same red lane: codec_base64_roundtrip_64k is a REAL +7.8% regression per 3ISWHyP7's A/B, so fixing the gate's noise handling alone still leaves the release refused on this scenario. A green organic release needs both.