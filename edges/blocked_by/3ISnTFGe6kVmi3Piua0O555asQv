the split cannot cold-build until a release and pin bump carry the directory-aware mechanism this item lands — proven by run 33033225229, where the pinned binary applied no patches over the split layout