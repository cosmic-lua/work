socket.tl:437's cast (SO_RCVTIMEO/SNDTIMEO timeval shape) can't close without a definitions.lua change in cosmopolitan + a pin bump; the spec's baseline instructions assume all 4 named sites close, which breaks as written until this lands