3IUBNQZZ adds format_identity, widens format to format(deltas, base?, cur?), and converts the five compare.format( call sites so both pt.Results reach the formatter; without that plumbing this item cannot print a header line at all, and cutting both in parallel puts two branches over the same header region and the same _perf/skew_test.tl wall (3IVF3HbV)