## Change

Evidence: retitling an item today requires a manual file edit plus commit (done once, 2026-08-29, for 3IbDFQzq's direction change — commit "retitle 3IbDFQzq for the tail-8 direction"). The workaround rule says the missing verb becomes an item: add a `retitle ID TITLE` verb (or fold into an existing write verb) that rewrites the title field as one CAS-published board mutation.
