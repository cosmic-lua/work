`skills/work/SKILL.md:332` on `main` names the bare command form `gitboard spec
ID FILE` in the slice loop's re-measure step, and `skills/optimize/SKILL.md:275`
says "`gitboard spec` replaces the sidecar". After PR #1461 lands on `board`,
`spec` refuses a write onto an item that already has a sidecar unless it also
carries `--base FILE` holding the text the caller read — so a session following
step 2 literally gets a refusal, and the second sentence describes a verb that
no longer does what it says. Both are one-line edits on `main`, which the board
branch's own change cannot make: they are different branches and different pull
requests. The refusal teaches the fix, so nothing is lost silently, but a skill
whose worked command does not run is the friction the enablement chapter exists
to remove.
