## Evidence

Observed while implementing SzFj_aL8o, 2026-09-12, cosmic a327ff32760ec15bbde7514c1cd7ed34647ae8d7 and unchanged by its PR1838. `_perf/bench/child_bench.tl:15` hardcodes child.run({"/bin/true"}). On this macOS host `ls -l /bin/true /usr/bin/true` prints `/bin/true: No such file or directory`, while `/usr/bin/true` exists. With pinned Cosmic SHA256 10f66af3cfe6b55e3f97c058ddff5e6b0ba3faf6eef8c2462cb7372895e4e1c2, `child.run({"/bin/true"})` returns `nil, exec failed: ENOENT: No such file or directory`; `child.run({"/usr/bin/true"})` returns code0. The local full gate's `_perf/perf_test.tl` failure is therefore a host path assumption, not a fuzz diagnostics regression. Exact-head Linux CI for PR1838 passes all five stages. This finding is outside that item's frozen scope and no benchmark was modified.

Search before filing: gitboard find "/bin/true" found only ended9kMa_JTLB and Apfg_iqa2 (C argv/exec tests), not this benchmark. Refine a portable external minimal-executable resolution rule that preserves the benchmark's explicit separation from Cosmic runtime startup and uses one chosen executable consistently across an A/B; do not replace it with a Cosmic startup probe or silently skip its functional check.
