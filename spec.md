Imported from whilp/cosmic#1215.

While verifying PR #1214 (issue #1200) from a clean, unambiguous clone (avoiding the
`COSMIC_MAKE_ROOT` nested-worktree interaction filed as #1212), a coverage baseline
regeneration (`bin/cosmic --make coverage --baseline`) measured `cosmic/fs/octal.tl`
at `2 4` (2 covered of 4 total lines, 50%) instead of the committed
`.cosmic-coverage` floor&#39;s `cosmic/fs/octal.tl 2 2` (2 of 2, 100%). This is a
*total*-line-count mismatch, not just a covered-line difference — the same
`.tl` source apparently resolves to a different executable-line count depending
on the measuring environment. Reproduced twice in the same clean clone. Not
caused by PR #1214&#39;s own diff (`cosmic/fs/octal.tl` is untouched by that change)
and not committed to the baseline — the implementer deliberately did not run a
full `--baseline` rewrite in order to avoid freezing this drift into the
committed floor, and instead hand-added only the one new row for `_make/vcs.tl`.

Possibly related to #1205 (bench coverage measurement disagreeing between CI and
local) as an instance of the same underlying class — coverage measurement giving
different results across environments — but the mechanism looks different: #1205
is about *covered*-line variance tied to a timing-bounded smoke pass, this is a
*total*-line variance on a file with no timing dependency at all. Worth a planner
triaging whether these are one root cause or two.


---
_Generated by [Claude Code](https://claude.ai/code)_