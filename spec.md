# friction: 2026-09-04 work9 (/work 9 --routine)

## orchestrator
- goal: claim «sxzp_M1yR» (board pin bump) via `take`, expecting the branch to fork from the base its own spec names ("the PR lands on this repository's board branch").
  actually happened: first `take` succeeded but reported "branch 3IrfJ5s6 off main" — the item carried no explicit `base` field, so `take` silently defaulted to main (cosmic-lua/cosmic's default base) even though the spec text says board. Cost: one extra `drop --session ... --why`, one `set --base board`, and one re-`take` (3 extra verb calls) to correct before any agent was spawned.
  contributed: the item (items/3IrfJ5s62kkTyyzSWGqsxzpM1yR.tl) had no `base`/`repo` key set at file time — the filer never ran `gitboard set --base board`, and `take`/`brief` do not cross-check the spec prose against the base default. This is the exact gap another open todo item, «HlNE_YWL2» ("graph: require repo/base explicitly on new --parent and attach, and refuse brief's silent base guess"), already targets.
  improvement: land «HlNE_YWL2» — a gate in `new`/`attach`/`brief` that refuses a silent base guess is higher leverage than a doc note, and it already exists as a spec-bar-passing item; no new item needed, just priority. Orchestrators should also spot-check `show ID`'s base line against spec prose ("lands on the board branch" / "this repository") before spawning when an item concerns the board's own machinery.
- goal: claim «mzpu_OKna» (review-brief text fix, board-branch tooling) via `take`.
  actually happened: same silent-base-guess bug as «sxzp_M1yR» above — `take` reported "off main" for an item with no explicit base whose spec clearly concerns board's own `_work/brieftext_review.tl`. Same 3-call repair (drop --session, set --base board, re-take).
  contributed: same root cause — item filed without `gitboard set --base board`/`--repo` at file time. Two independent hits in one pass corroborate «HlNE_YWL2» is worth prioritizing, not a one-off.
  improvement: same as above — prioritize «HlNE_YWL2»; already filed and spec-bar-passing, so filing a duplicate item is unwarranted. Noting here purely as corroborating evidence for its priority.
- goal: emit builder briefs for «sxzp_M1yR» and «mzpu_OKna» to hand to agents.
  actually happened: both briefs were generated (`gitboard brief builder ID`) BEFORE the base-guess bug fix (`set --base board`) was applied, so the captured brief text said "branched off origin/main" and "Open a PR from X to main" — wrong target branch. Caught by re-reading the filled brief before spawning rather than pasting it blind; both briefs were regenerated after the `set --base board` fix, this time correctly saying "board". Cost: 2 extra `brief builder` calls plus the re-read; would have cost a full bounced PR (wrong base branch, review round burned) had it not been caught.
  contributed: `gitboard brief` renders from the item's CURRENT base field at call time with no staleness warning, so a `brief` issued before a `set --base` repair silently bakes in the stale target. This compounds the same root cause as the two entries above («HlNE_YWL2»'s silent-base-guess gap) — a `set --base` repair should probably invalidate/warn about any already-captured brief text, though that is a smaller, less urgent gap than the base-guess itself.
  improvement: procedural for now — always regenerate (never reuse) a captured brief after any `set`/`spec` repair on the same item, and read the final filled brief once before pasting it into an agent prompt rather than trusting the placeholder-fill script blindly. Landing «HlNE_YWL2» removes the root cause that makes this necessary in the first place.

## build 1iOZ_4iqj (general-purpose) — no PR (already satisfied), 154s, 13 tool calls
- goal: implement «1iOZ_4iqj» — rewrite `cosmic/time.tl`'s `sleep_remaining_ms` for `unix.nanosleep`'s new return shape, once a pin bump carrying it lands.
  actually happened: transcript (`_tool/friction.tl`): wall=154s, tool_calls=13 (Bash=12, Read=1), tokens in=28 out=1359 cache_read=732016 cache_create=71049, no edits (first edit: none), 0 errors, 0 repeated commands. The agent found the entire `## Change` already implemented and merged via main PR #1705 ("cosmos pin bump ... exact-contract adaptation"), which had batched this exact fix in with ~23 other affected files. It verified independently (grep for `unix.nanosleep` call sites, read the generated type declaration, ran the full `bin/cosmic --make ci` gate) rather than trusting the match, then reported cleanly with zero diff and no PR — a fully correct outcome, not wasted effort, but the full build+gate cycle (13 calls, ~154s) was spent purely on verification of a pre-existing fix.
  contributed: the item was filed speculatively ahead of a pin bump ("once pinned"), and the pin-bump PR that landed the fix (#1705) never cross-referenced this board item id, so nothing signaled to a picker that the item might already be moot before a full verify-and-build pass.
  improvement: (agent's own account, quoted) "noting the item id in that PR's description (or closing the item at merge time) would let a builder short-circuit on `git log -- <file>` alone instead of a full build+gate cycle to confirm." Lower-leverage than a gate, but plausible: a convention that a pin-bump PR touching a file a pending item names cites the item id. Orchestrator's own handling: closed the item directly (`done ID --reason completed --force --why ...`, since there was no PR to point at) rather than leaving it `doing` with no PR — this is itself the "research handover" pattern (`take ID --result`) applied to a builder outcome; worth folding into doctrine as a named case ("builder found the goal already met upstream") rather than leaving it implicit.

## build sxzp_M1yR (general-purpose) — PR #1706, 170s, 31 tool calls
- goal: implement «sxzp_M1yR» — bump the board branch's bin/cosmic.pin past the coverage --baseline guard, regenerate .cosmic-coverage.
  actually happened: opened https://github.com/cosmic-lua/cosmic/pull/1706 cleanly, gate green, mutation-tested the guard. But for its mutation-test CONTROL, it read from and wrote into `/home/user/cosmic/o/board` — the ORCHESTRATOR's own live board worktree, not its own `/home/user/agent-work/sxzp_M1yR` worktree — running `--baseline` there with COSMIC_COVERAGE_ENV unset (which rewrote `.cosmic-coverage` in that worktree) before restoring it with `git checkout -- .cosmic-coverage`. It happened to land while the orchestrator's board worktree was between gitboard verb calls and self-restored correctly (verified clean after the fact), but it is a shared, live checkout other concurrent sessions and the orchestrator itself read/write board state through — a genuinely concurrent write there could have raced a `gitboard` commit or been read mid-mutation by another session.
  contributed: the brief's "Your git worktree is already checked out at: <path>. Work exclusively inside that directory" line does not explicitly forbid reading/writing OTHER paths outside it for incidental purposes like a mutation-test control; the agent treated an already-built sibling worktree as free scratch space because it was convenient (zero rebuild cost) rather than because it was safe to touch.
  improvement: strengthen the brief's "Where to work" instruction (or the `## What to do` mutation-test step) to say explicitly: never read from or write to any git worktree other than your own, including one that looks idle — use `git worktree add` for a second scratch checkout if a control/comparison needs one. Board-tooling item, in scope for whichever of the already-filed brief-template items (e.g. «qWfP_VKKJ», "briefs: the agent's worktree is the only tree it reads") ends up covering this; not filing a new item since qWfP_VKKJ's title already covers exactly this shape of mistake, filed before this pass.

## build mzpu_OKna (general-purpose) — PR #1707, 221s, 48 tool calls
- goal: implement «mzpu_OKna» — add four sentences to the REVIEW brief template's fetch paragraph, plus a pin test.
  actually happened: transcript: wall=221s, tool_calls=48 (Bash=32, Edit=6, Read=8), first edit call 9, 1 error (a `which cosmic` probe that returned exit 1, harmless), 4 repeated-command groups (mostly re-reading/re-running the same test file while iterating). Agent's own account: a plain substring `find` across a line-wrapped template sentence failed silently until it found and used the file's own `flat()` test helper (built for exactly this); separately, placed a new test function before its own dependency (`flat`) was defined further down the file, passed type-check, failed at runtime, cost one extra `--make test` round-trip to relocate it.
  contributed: both are agent-side authoring mistakes, not spec/tool gaps — the tree already documents the fix for the first (a doc comment on `flat()`), and the second is an ordinary Lua top-to-bottom scoping surprise the agent diagnosed and fixed itself in one extra call.
  improvement: none needed — self-corrected within budget, no repeat pattern across other agents this pass.

## build 4B0h_t06K (general-purpose) — PR #1708, 211s, 22 tool calls
- goal: implement «4B0h_t06K» — one doc-comment line on item.tl's builders field.
  actually happened: transcript: wall=211s, tool_calls=22, first edit call 10, 0 errors, 0 meaningful repeats. Clean run; agent correctly declined to fabricate a mutation test for a doc-only change (spec's own step 5 assumes a guard exists, this item added none) and said so explicitly rather than padding. Flagged, out of scope, that the field's wire-shape was already partially documented nearby (just not in `decode`-facing terms) — a real, useful observation, correctly not acted on.
  contributed: n/a — no friction.
  improvement: none.

## build VbI3_FiHP (general-purpose) — PR #1709, 289s, 53 tool calls
- goal: implement «VbI3_FiHP» — doc `--session`'s always-pass-it rule in three files plus two pinning tests.
  actually happened: transcript: wall=289s, tool_calls=53 (Bash=30, Edit=7, Grep=6, Read=8), first edit call 20, 0 errors, several re-read/re-`wc -l` repeats while packing text into an at-cap file. Agent's own account: the spec's Evidence section quoted `help take`'s wording (which actually lives in `_work/gitboard.tl`'s CLI flag table) while the Change section named a different file (`_work/gitverbs.tl`) carrying a parallel doc comment — cost ~3 tool calls chasing the quoted string before trusting the Change section's file name literally. Separately, `_work/gitgate.tl` and `_work/gitverbs.tl` were BOTH already sitting exactly at the 500-line cap before this item started; fitting the addition took 3 edit/`wc -l` iterations to repack onto fewer lines.
  contributed: the spec quotes evidence text from one file while naming a different file to edit, with no explicit note that they're different files carrying parallel wording — an ordinary spec-writing gap, not a tool bug. The at-cap file discovery is exactly what the "before the first edit, `wc -l` every file" step exists to catch, and it worked — cost was the repacking, not a missed cap.
  improvement: (agent's own suggestion) specs that name Evidence quotes should cite the exact file:line they were quoted from, matching the precision `_work/gitgate.tl:268` already uses elsewhere in the same spec — an authoring-time convention, not a new item; too narrow to file on its own.

## build LmNB_gMXm (general-purpose) — PR #1710, 337s, 46 tool calls
- goal: implement «LmNB_gMXm» — add a sleep-wait warning paragraph to three brief templates (builder/review/research).
  actually happened: transcript: wall=337s, tool_calls=46 (Bash=30, Edit=6, Read=7, Write=1), first edit call 16, 0 errors, moderate repeats while iterating across three template files. Agent's own account: the spec's own file-discovery hint (`git grep -rn "run the repo's gate locally" _work/`) matched only `_work/brieftext.tl`, NOT `_work/brieftext_review.tl` — even though the spec's own Change section names `gitboard brief review` as a third recipient template. The agent had to read `brief.tl`'s KINDS/dispatch logic to work out that "review" resolves to either `REVIEW` or `RESEARCH_REVIEW` depending on item state, and that both needed the new paragraph. Also discovered (via `ls`/`find`, not documented anywhere) that `_work/brieftext_review.tl` had NO existing pin-test file at all, unlike `brieftext.tl`.
  contributed: the spec's own suggested discovery command (a grep for one literal phrase) undercounted the templates its own Change section named — a real spec/evidence mismatch, not just an agent misreading. **This is the same root shape as the sxzp_M1yR/mzpu_OKna base-guess bug and the VbI3_FiHP evidence-file mismatch above: specs whose own measured "how to find this" commands don't actually cover everything the Change asks for.**
  improvement: no new item — folds into the same authoring-discipline gap already surfaced twice above (cite exact files/anchors, not a grep expected to find them all). Operationally significant: **this PR (#1710) and mzpu_OKna's PR (#1707) BOTH ended up editing `_work/brieftext_review.tl`** — mzpu_OKna added its REVIEW-template sentences, LmNB_gMXm added a RESEARCH_REVIEW-template paragraph and a NEW test file `_work/brieftext_review_test.tl`. This is a real file overlap the orchestrator's own pre-wave disjointness check MISSED (see orchestrator entry below) — worth a merge-conflict check before landing #1710 second.

## build gx09_qUkP (general-purpose) — no PR (already satisfied), 191s, 8 tool calls
- goal: implement «gx09_qUkP» — resolve the register_extension pin-bump question, per the orchestrator's added re-verify-from-scratch instruction.
  actually happened: transcript: wall=191s, tool_calls=8, 0 errors, 0 repeats — an efficient, decisive run. Confirmed from scratch (ancestry check, generated-types grep, fresh build, full `--make ci`) that main's current pin already satisfies the item's target; correctly produced no PR (a no-op diff) and said so rather than opening an empty PR. Agent's own account: the spec document itself carries two internal rounds of self-correction ("Update", "Superseded by", "Current Change" replacing an earlier one) that had to be read past before reaching the orchestrator's own terminal instruction — not costly here (efficient run regardless) but a real readability tax.
  contributed: this is an artifact of iterative spec-writing on a long-lived question item, not a tool gap.
  improvement: none actionable at gate/doc leverage — noting only because it's the kind of thing a `spec`-repair pass (trimming a resolved item's superseded history once closed) could clean up; not worth its own item for one observation.
- goal: fill placeholders in the `gitboard brief review LmNB_gMXm` output before spawning the reviewer.
  actually happened: the verdict line said "fill <PID>, then paste the body verbatim" — but `<PID>` only appears inside a quoted shell example INSIDE the item's own spec text (`while kill -0 <PID> 2>/dev/null; do sleep 5; done`, itself the diff being reviewed), not a real templating placeholder meant for the orchestrator to substitute. Caught by reading the brief before spawning rather than trying to guess a PID to fill in; pasted the brief unmodified.
  contributed: `gitboard brief`'s placeholder-detection appears to pattern-match any `<UPPERCASE>` token anywhere in the rendered body, including inside a quoted spec excerpt that itself demonstrates the very thing this PR's item is about (warning agents against literal-looking placeholders in shell examples) — a false positive specific to specs that quote shell syntax using angle-bracket placeholders.
  improvement: the placeholder scanner should exclude text inside the spec's own verbatim block (between the `---` markers), or restrict detection to a fixed allowlist of real placeholders (`<WORKTREE>`, `<BOUNCE_CONTEXT>`, `<PID>` only when it is the brief template's OWN token, not the spec's). Low cost this time (one read, no wrong action), but flagging since it will recur on any future item whose spec quotes shell code with angle-bracket examples. Not filing separately — in scope for whichever brief-fill item already covers grammar edge cases.

## review sxzp_M1yR (general-purpose) — accept, merged 90f38ade, 171s, 31 tool calls
- goal: fresh-context review PR #1706 (board pin bump) and land it if it passes.
  actually happened: transcript: wall=171s, tool_calls=31 (Bash=20, 3x ToolSearch, GitHub MCP calls), 1 error: `mcp__github__enable_pr_auto_merge` failed with "Protected branch rules not configured for this branch" (GraphQL) — the reviewer's first instinct on `accept`, following the brief's generic "enable auto-merge (squash), never merge directly" line, which doesn't distinguish a board-branch PR (no branch protection, merges directly per `gitboard help review`) from a main-repo PR (protected, needs auto-merge). One extra tool call and a `gitboard help review` lookup resolved it correctly.
  contributed: `gitboard brief review`'s "Recording your verdict" section states the auto-merge instruction unconditionally ("a main-repo PR lands by enabling auto-merge... never by merging directly") without noting the board-branch exception that `gitboard help review` documents separately — a spec/doctrine-surface split the reviewer had to bridge themselves.
  improvement: (agent's own suggestion, and matches its own diagnosis) branch the brief's accept instructions on target ref (board vs main), or have `gitboard verdict`'s accept output itself name the correct landing method for the item's own recorded base — a gate/tool-level fix, higher leverage than a doc note. Not filing a new item — likely already covered by one of the already-filed brief-template items in this cluster; flagging for whichever refiner triages the friction log to fold in if not already covered.

## build nwzb_73yW (general-purpose) — PR #1711, 502s, 75 tool calls
- goal: implement «nwzb_73yW» — rewrite the roots-carry-no-repo refusal message, add a --repo help note, pin both in gitgraph_test.tl.
  actually happened: transcript: wall=502s (longest of the wave), tool_calls=75, first edit call 32, 3 errors, 7 repeated-command groups. Two of the three errors were the SAME file-length violation hit twice in a row (`_work/gitgraph_test.tl` at 505 then 501 lines against a 500 cap) while iterating to fit the new test into an already-tight file (493/500 before this item started) — cost ~10 minutes of `wc -l`/`--check lint` round trips before folding the new assertions into an existing test function instead of writing a new one. The third error was a deliberate manual probe (`gitboard init` in a scratch dir to see the refusal live) that correctly produced exit 1 as expected, not a real mistake.
  contributed: same shape as the VbI3_FiHP entry above — a shared file already sitting at or near its 500-line cap before the item's own change, with the item's spec/evidence not flagging that tightness up front.
  improvement: same as VbI3_FiHP's entry — recurs a second time in this pass on a DIFFERENT file (`gitgraph_test.tl` vs `gitgate.tl`/`gitverbs.tl`), reinforcing that this `_work/*_test.tl` cluster is systematically near its cap. Worth a standing item: a `wc -l` note (or lint warning) surfaced automatically when a `## Change` targets a test file already within ~10 lines of the 500 cap, so specs are written with headroom in mind rather than discovered by the builder. Filing this as one item below.

## review mzpu_OKna (general-purpose) — accept, could not land (board branch auto-merge), 171s, 29 tool calls
- goal: fresh-context review PR #1707, land it if accepted.
  actually happened: verdict recorded accept correctly via `gitboard verdict`; `enable_pr_auto_merge` failed twice with "Protected branch rules not configured for this branch" (board has no branch protection). Per its brief (spawned BEFORE the orchestrator had corrected this instruction — see sxzp_M1yR review's friction entry above, which surfaced the fix only after this agent was already dispatched), the agent correctly stood down rather than merging directly, and flagged it in its final report instead of guessing. Orchestrator merged PR #1707 directly (squash, sha 83344664) and closed the item.
  contributed: same root cause as sxzp_M1yR's review entry — the brief's accept instructions don't branch on target ref (board vs main). This is now corroborated a SECOND time in the same pass, independently, by an agent that had no way to know about the first occurrence. Confirms the fix belongs in the tool/brief, not in per-agent instructions the orchestrator has to remember to add by hand each time (which is what was done for the later nwzb_73yW review, ad hoc, as a stopgap).
  improvement: same as sxzp_M1yR's entry — branch `gitboard brief review`'s accept instructions (or `gitboard verdict`'s accept output) on the item's recorded base. Filing this as a concrete item below since it now has two independent corroborating occurrences plus a clean root-cause diagnosis both agents converged on independently.

## review 4B0h_t06K (general-purpose) — request-changes, 171s, 17 tool calls
- goal: fresh-context review PR #1708 (one-line doc comment).
  actually happened: clean, efficient review (0 friction). Correctly caught that the new line duplicated an existing, adjacent doc-comment sentence saying the same thing in different words — a real quality finding a less careful review would have waved through since the diff technically matched the letter of `## Change`. Posted a precise, quoted, actionable comment; correctly declined to fabricate a mutation test for a no-guard doc change.
  contributed: n/a — this is the review process working as intended (adversarial posture catching a real, if minor, defect).
  improvement: none needed. Orchestrator note: reworking this bounced item exposed a real tool gap — `gitboard brief builder ID` on an item in `rework` state returns the SAME template as a fresh pull (`<BOUNCE_CONTEXT>` renders empty, no mention of the existing PR, the reviewer's comment, or "push to the same branch, don't open a new PR"). The orchestrator had to hand-write the rework brief from scratch (fetching the PR comment via GitHub MCP, writing explicit reuse-the-same-branch instructions) rather than trusting the emitted brief — this is exactly the gap board item «qgFj_DaXr» ("builder brief: auto-fill the rework hand-back (BOUNCE_CONTEXT, worktree reuse, push-not-new-PR) from the item's own verdict/pr") already targets; corroborating evidence for it, no new item needed.

## review LmNB_gMXm (general-purpose) — accept, could not land (board-branch auto-merge, THIRD occurrence), 217s, 36 tool calls
- goal: fresh-context review PR #1710, land it if accepted.
  actually happened: thorough review (verified live brief rendering, mutation-tested, checked file lengths), verdict accept recorded correctly. Same `enable_pr_auto_merge` "Protected branch rules not configured" failure as sxzp_M1yR and mzpu_OKna's reviews — THIRD independent occurrence this pass (this agent was also spawned before the ad hoc board-vs-main note existed). Went further than the other two: sampled 20 recent closed board-branch PRs via the API and found they mostly show `merged:false` despite having landed, and correctly flagged the landing-mechanism ambiguity to the orchestrator rather than guessing. Orchestrator merged PR #1710 directly (squash, sha 4f6459ea) and closed the item.
  contributed: same root cause, third corroboration. Also surfaced a secondary, real question (why do many historical board PRs show merged:false in the API despite landing?) that the orchestrator did not chase down this pass — noted as an open question, not resolved: cosmic-lua/cosmic's `board`-PR landing convention prior to this session's direct-merge approach is unconfirmed; possibly a different close mechanism (manual push + close-without-merge) was used historically. Orchestrator verified its own direct-squash-merges (PRs #1706, #1707, #1710, #1711) all show `merged:true` correctly, so today's mechanism is sound regardless of what history shows.
  improvement: same fix as filed below (branch the brief/verdict output on target ref). The historical merged:false pattern is a separate, smaller curiosity — not filing an item for it; flag only if it recurs as a real problem.

## review VbI3_FiHP (general-purpose) — request-changes, 237s, 36 tool calls
- goal: fresh-context review PR #1709.
  actually happened: clean, precise review (0 errors, 1 trivial repeat). Live-verified the ACTUAL rendered output of `gitboard help take` against the PR head (not just the diff) and caught that the builder edited the wrong file — a Teal `@param` doc comment in `_work/gitverbs.tl` that is not what `gitboard help take` renders — while the real fix belonged in `_work/gitboard.tl`'s CLI option table. This is exactly the kind of adversarial, "does it actually serve the outcome" check the review posture calls for, and it caught a defect a pure diff-scope check would have missed (the diff WAS in-scope by file name, just the wrong file for the actual behavior).
  contributed: n/a — review process working as intended, a second real catch this pass (alongside 4B0h_t06K's).
  improvement: none needed for the review itself. Same underlying spec-writing gap noted in the LmNB_gMXm build entry above (a spec describing a target by role/description when multiple files could match) — no new item, corroborates the existing observation.

## review nwzb_73yW (general-purpose) — accept, merged 776c9f1 directly (correctly), 177s, 31 tool calls
- goal: fresh-context review PR #1711, land it if accepted — this review's brief included the orchestrator's ad hoc board-vs-main merge-method correction (added after the sxzp_M1yR review surfaced the gap).
  actually happened: clean review, accept, and merged DIRECTLY without hitting the auto-merge dead end — the ad hoc brief correction worked as intended. Also independently found `mcp__github__search_code` returns zero results for known-present strings in this repo (unindexed/private?) and fell back to a real worktree+grep, costing 2 tool calls but reaching a more reliable result anyway (it also enabled the mutation test).
  contributed: confirms the fix belongs in the tool/brief (as the filed item below proposes) — a per-agent hand-added note is sufficient to prevent the friction, validating that approach as the right shape for the fix, just wrongly scoped to per-agent instead of built into `gitboard brief`/`verdict` itself.
  improvement: folds into the item filed below. Separately, worth a note for whoever files it: `mcp__github__search_code` appearing to return nothing for this repo is worth a one-line mention in review doctrine (prefer a fresh worktree + grep over GitHub code search here) — minor, not filing its own item, low cost observed (2 calls) both times it happened (once here, implicitly also true for other reviewers who went straight to worktree+grep without trying search first).

## build 4B0h_t06K rework (general-purpose) — pushed 8f75a8cb, 53s, 11 tool calls
- goal: rework PR #1708 per the review's fold-don't-duplicate ask.
  actually happened: fast, clean, precise rework (53s, 11 calls, 0 errors) — the orchestrator's hand-written rework brief (quoting the reviewer's exact comment and giving explicit push-to-same-branch instructions, since `gitboard brief builder` on a rework item doesn't auto-carry any of that) was sufficient; the agent needed no clarification and landed the fix first try.
  contributed: n/a for the agent itself — the orchestrator's manual rework-brief construction is the point of friction, already logged above under 4B0h_t06K's review entry (folds into «qgFj_DaXr»).
  improvement: none beyond what's already noted.

## build VbI3_FiHP rework (general-purpose) — pushed 29791256, 137s, 43 tool calls
- goal: rework PR #1709 per the review's move-the-fix-to-the-right-file ask.
  actually happened: clean rework, landed first try, mutation-tested and live-verified. 3 harmless probe errors (grep for the CLI dispatch helper in the wrong candidate files/paths before finding `gitboard.tl:203`'s option table directly) — cost 3 tool calls, no real friction.
  contributed: minor; the agent's own search for "where does help text render from" cost a few exploratory greps, same class as the LmNB_gMXm build's discovery cost.
  improvement: none beyond what's already noted for the shared root cause (spec naming a target by description rather than exact file:line when ambiguity exists).

## review 4B0h_t06K round 2 (general-purpose) — accept, merged directly, 77s, 13 tool calls
- goal: verify the round-1 fold-don't-duplicate rework actually fixed the issue, land if so.
  actually happened: fast, clean, 0 friction. Correctly merged directly since it was briefed with the board-vs-main correction; verified the rework against actual code (grepped decode/encode call sites to confirm the doc comment's claim was factually accurate, not just "looks fixed").
  contributed: n/a.
  improvement: none.

## review VbI3_FiHP round 2 (general-purpose) — accept, merged directly, 147s, 33 tool calls
- goal: verify the round-1 wrong-file fix was corrected, land if so.
  actually happened: thorough, 0 friction. Live-verified `gitboard help take` and `gitboard help orchestrate` output against the rebuilt PR head (not just the diff), confirmed the fix landed in the right place this time, full mutation test, merged directly without hitting the auto-merge dead end (briefed with the correction).
  contributed: n/a.
  improvement: none.

## orchestrator (closing the pass)
- goal: file a countermeasure item for the board-vs-main merge-method gap (3 corroborating occurrences this pass).
  actually happened: `gitboard new "review brief: branch accept-landing instructions on the item's base (board vs main)" --parent ... --spec-file ...` succeeded (handle 44MJ_k4Jp) — but it was a near-exact duplicate of an ALREADY-OPEN item, «4CTF_fCZV», filed earlier the same day (13:08:09Z) with the identical title and root cause, discovered only after filing because `gitboard help new`'s doctrine ("search first... cite the existing item rather than duplicate it") was not followed before this call. Cost: one wasted `new` call, then a `spec --base` amend to fold this pass's 3 additional corroborating occurrences into 4CTF_fCZV instead, then a `done --reason not-planned --force` on the duplicate, citing the merge — 3 extra verbs total to fully repair.
  contributed: the closing-pass procedure (`skills/work/friction.md`) says to file countermeasures "now" but doesn't itself instruct a search-first step the way `gitboard help build`'s "Out-of-scope findings" section does (`grep -ril '<phrase>' items/` before `new`). The orchestrator knew the doctrine (quoted it correctly when reasoning about builder/reviewer out-of-scope findings during the pass) but didn't apply the same discipline to its own end-of-pass filing.
  improvement: procedural, for future passes: always `grep -ril` for a countermeasure's key phrase in `items/*.md` before `gitboard new`, even (especially) when confident it's novel — this pass's own friction log already names three separate items (`HlNE_YWL2`, «qgFj_DaXr», «4CTF_fCZV») that existed BEFORE this pass started and covered gaps this pass independently rediscovered; a systemic pattern of not searching first, not just a one-off. Not filing a new item for this — it is exactly what `skills/work/friction.md`'s own closing-pass instructions should already prevent, and the fix is discipline, not a new gate.

## review 4B0h_t06K-r2 (orchestrator, no agent spawned)
- goal: claim the round-2 review of PR #1708 under a self-invented round-numbered label (`review-4B0h_t06K-r2-d6f1cc41`), reasoning that round 1 already held `review-4B0h_t06K-d6f1cc41` and a distinct label per round seemed right.
  actually happened: `take` under that label succeeded, but the brief `gitboard brief review 4B0h_t06K` had already been generated naming `review-4B0h_t06K-d6f1cc41` (round 1's label, unchanged) as the string the pasted body would tell the agent to use for `gitboard verdict`. Caught immediately, before spawning any agent: dropped the mis-claimed session (`drop --session review-4B0h_t06K-r2-d6f1cc41`) and re-took under the label the brief actually names. No agent ever ran under `-r2-`; zero cost beyond the two extra verb calls.
  contributed: exactly the gap board item «l4mQ_rPh2» ("gitboard brief review: mint a round-numbered session label instead of always round 1's") already targets — `gitboard brief review` always mints/reuses round 1's session string regardless of how many prior rounds the item has been through, so an orchestrator's own natural instinct (round-number the label) collides with what the brief will actually tell the agent to authenticate as.
  improvement: land «l4mQ_rPh2» (already filed, spec-bar-passing) — or, until it lands, always read `gitboard brief review`'s own printed session label and claim under EXACTLY that string rather than inventing one, which is what this pass did after this single self-correction. No new item; this is corroborating evidence for one that already exists.
