## Change

Evidence (re-review of PR 1518, 2026-08-29): a bounce can be cured by a change OUTSIDE the PR — 1518's prose became true when the tool fix (PR 1520) merged, with the PR's head AND spec both unmoved — but the head-already-judged guard in `_work/gitverdict.tl` refuses to record any verdict on an unchanged head+spec pair, so the accept was unrecordable until the reviewer force-rewrote the spec to append a Rework note. The decision, settled 2026-08-29: the doctrine STAYS "record the cure on the spec" — it worked in both live occurrences (1518, 1519), keeps the verdict pair (head, spec revision) as the complete judgment record, and needs no new guard surface. The change is the refusal text only: when gitverdict refuses an unchanged head+spec pair, the message names the way forward — append what changed outside the PR to the item's spec (a short Rework note via `gitboard spec`), then record the verdict against the new revision. Measure the current refusal text first (`grep -n "already judged" _work/gitverdict.tl`), extend it, and pin the new text with a substring assertion in `_work/gitverdict_test.tl`; mutation-verify the assertion goes red against the old text.
