# friction: 2026-09-06 work4 (attended; reconstructed 17:46Z after the scratchpad was deleted — prose condensed, numbers regenerated from transcripts)

## agent transcripts (friction.tl)
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a0717acf62c602fda.output: events=137 tool_calls=33 wall=240s tokens: in=68 out=228 cache_read=2302756 cache_create=46004 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a1071efed5fea2311.output: events=190 tool_calls=51 wall=451s tokens: in=88 out=1727 cache_read=3762027 cache_create=79157 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a10f4386d06f6106e.output: events=127 tool_calls=30 wall=361s tokens: in=62 out=456 cache_read=2589430 cache_create=68197 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a181a4999e6601e5d.output: events=486 tool_calls=124 wall=4682s tokens: in=252 out=2390 cache_read=21439126 cache_create=388227 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a1c5ddc0bb9ddea39.output: events=163 tool_calls=40 wall=349s tokens: in=78 out=2209 cache_read=3130658 cache_create=69018 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a24f818ad42f03c23.output: events=351 tool_calls=90 wall=978s tokens: in=182 out=4387 cache_read=11050579 cache_create=139734 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a28749dbe42f11e1b.output: events=125 tool_calls=30 wall=318s tokens: in=58 out=134 cache_read=2314295 cache_create=65702 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a32429e8b67a0476e.output: events=515 tool_calls=132 wall=1640s tokens: in=268 out=3735 cache_read=21449045 cache_create=213955 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a3b9015a1896da705.output: events=220 tool_calls=54 wall=620s tokens: in=110 out=351 cache_read=4722244 cache_create=111008 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a3c638fdb9d7d11f2.output: events=171 tool_calls=44 wall=410s tokens: in=84 out=479 cache_read=4093262 cache_create=83035 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a416090cee6f44118.output: events=107 tool_calls=26 wall=173s tokens: in=48 out=139 cache_read=1674017 cache_create=47772 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a421b20578933b90e.output: events=122 tool_calls=30 wall=288s tokens: in=62 out=392 cache_read=2411141 cache_create=60035 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a4863d54302532e9f.output: events=199 tool_calls=49 wall=433s tokens: in=96 out=610 cache_read=4588465 cache_create=92267 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a4b81934a32088504.output: events=147 tool_calls=37 wall=405s tokens: in=70 out=210 cache_read=2704782 cache_create=58169 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a510dda6aa5f75765.output: events=664 tool_calls=173 wall=1846s tokens: in=348 out=2274 cache_read=30625281 cache_create=234028 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a520225e7c18c8abe.output: events=103 tool_calls=25 wall=166s tokens: in=48 out=233 cache_read=1591915 cache_create=40698 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a579f79ca4d4328ce.output: events=533 tool_calls=135 wall=2897s tokens: in=276 out=1482 cache_read=21902792 cache_create=502553 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a620c21c5e93049eb.output: events=98 tool_calls=23 wall=192s tokens: in=44 out=104 cache_read=1480714 cache_create=42886 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a6580dc8aaca2428e.output: events=414 tool_calls=108 wall=1058s tokens: in=218 out=1921 cache_read=14669768 cache_create=153174 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a6747cc29456489d9.output: events=141 tool_calls=35 wall=360s tokens: in=70 out=436 cache_read=2679697 cache_create=58534 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a6f559e0c099e5662.output: events=157 tool_calls=40 wall=371s tokens: in=76 out=262 cache_read=3262514 cache_create=78435 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a71319aae5b5ad71b.output: events=327 tool_calls=82 wall=759s tokens: in=168 out=3097 cache_read=8618840 cache_create=111154 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a716ac549f29c74e6.output: events=264 tool_calls=67 wall=426s tokens: in=136 out=1463 cache_read=5598573 cache_create=75404 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a7253d253a582ef26.output: events=141 tool_calls=35 wall=739s tokens: in=66 out=176 cache_read=2667509 cache_create=69077 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a789b363fab30bae7.output: events=158 tool_calls=41 wall=313s tokens: in=76 out=403 cache_read=3437575 cache_create=75102 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a7ffac4528e263695.output: events=90 tool_calls=21 wall=217s tokens: in=42 out=120 cache_read=1625456 cache_create=65429 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a8bc26b816618d4c5.output: events=111 tool_calls=27 wall=229s tokens: in=56 out=1983 cache_read=1862022 cache_create=45934 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a8e191603ba77ee58.output: events=107 tool_calls=26 wall=177s tokens: in=50 out=164 cache_read=1640215 cache_create=40250 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a8f1791039fb6c833.output: events=401 tool_calls=104 wall=1380s tokens: in=212 out=2845 cache_read=14111597 cache_create=158764 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a9bc1aa35dc9bfde7.output: events=43 tool_calls=10 wall=68s tokens: in=16 out=53 cache_read=484744 cache_create=35999 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a9c6fc44dc6808689.output: events=76 tool_calls=19 wall=150s tokens: in=32 out=232 cache_read=1094553 cache_create=44295 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a9f05f1681d19faca.output: events=118 tool_calls=29 wall=244s tokens: in=54 out=526 cache_read=2074799 cache_create=57746 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/aa1e3b5d56ee6d881.output: events=121 tool_calls=30 wall=246s tokens: in=56 out=425 cache_read=2050145 cache_create=51510 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/ab20a13c1f15c86cf.output: events=137 tool_calls=33 wall=241s tokens: in=66 out=979 cache_read=2309174 cache_create=48557 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/ab91920041b29bef7.output: events=327 tool_calls=85 wall=654s tokens: in=172 out=3519 cache_read=9206048 cache_create=111068 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/ab99dfaf840f54cca.output: events=182 tool_calls=45 wall=289s tokens: in=92 out=440 cache_read=4147375 cache_create=79339 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/ac07e642943c0589a.output: events=108 tool_calls=27 wall=204s tokens: in=48 out=354 cache_read=1745562 cache_create=51729 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/ac175f82107b4f8e2.output: events=127 tool_calls=31 wall=227s tokens: in=60 out=277 cache_read=2202340 cache_create=50710 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/ac56f5ec2291eee5c.output: events=205 tool_calls=51 wall=491s tokens: in=104 out=454 cache_read=5057862 cache_create=91348 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/ac88892e0031aceba.output: events=488 tool_calls=126 wall=1520s tokens: in=252 out=2659 cache_read=22972384 cache_create=245773 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/ad09e8c009060f8ea.output: events=142 tool_calls=35 wall=445s tokens: in=66 out=1294 cache_read=2970025 cache_create=84528 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/ad220b05a369f522d.output: events=337 tool_calls=86 wall=873s tokens: in=174 out=2758 cache_read=10517359 cache_create=142113 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/adef40e228cf46fd7.output: events=136 tool_calls=32 wall=278s tokens: in=66 out=945 cache_read=2371231 cache_create=54718 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/ae18a2f922512ba38.output: events=151 tool_calls=37 wall=307s tokens: in=72 out=455 cache_read=2917931 cache_create=65604 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/ae952d6688b2bd280.output: events=187 tool_calls=47 wall=973s tokens: in=98 out=600 cache_read=4435727 cache_create=168270 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/af0b166adca485b2a.output: events=144 tool_calls=34 wall=435s tokens: in=72 out=1095 cache_read=2940424 cache_create=66708 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/af3d7ea679f9f15cf.output: events=423 tool_calls=111 wall=1548s tokens: in=226 out=2422 cache_read=12529117 cache_create=240594 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/af5f00477104e6354.output: events=333 tool_calls=88 wall=891s tokens: in=178 out=2472 cache_read=9138956 cache_create=112058 
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/af63296c8397332b2.output: events=176 tool_calls=44 wall=444s tokens: in=86 out=477 cache_read=3729253 cache_create=78690 

## orchestrator (condensed; the original prose was lost with the scratchpad at 17:45Z — an agent's cleanup ran `rm -rf <scratchpad>` because its brief file lived there)
- pullable listing: `show` renders 8 of 209 todo rows; built a scratch query over o/board.db (8 calls). filed → landed as `show --todo N` (#48).
- head item «Hkal_OAFy» carried a stale "Blocked" in prose the bar cannot read; respec'd from measured facts (#1747 landed, pin at e748d6a1e); built and merged (#1750).
- specs naming files at the 500 cap unmeasured (main.tl 499, brief_test.tl 498, gitview.tl 497): filed «SmAQinD»; the `tight:` gate landed (#45), `absent:` paths (#51).
- every builder prompt needed a hand-added GitHub-reach paragraph; interim doc (#44), systemic fix specced (`take --open` «hrqC», `verdict` lands «x8CF»).
- handle misread on `new` (3 refused calls): fixed (#46). `done` refusal named a non-verb `land`: fixed (#50). `take --pr` refusal named --force instead of --session («zq2b_vYsf», open).
- take/review deadlock (pull refused because a diff with CI running awaits a verdict) hit 6 times; fixed for first pushes (#52), residual for re-pushes filed «4QFJ_HSRc» (ci_checks keyed by PR, not head).
- a builder's smoke test in a linked worktree pushed a stray item branch to the live board; ended not-planned; guard landed (#53).
- two orchestrators (this session and a36d13ac) worked the board; five refused review takes before learning the other held them: `show` now prints the reviewer (#57).
- `bin/gitboard` runs a pinned release: nothing landed in cosmic-lua/work is live until cosmic's pin moves — pin bumped to 2026-09-06-1ff3238 (#1756); #48/#55/#56/#57 need the next bump.
- orchestrator-authored spec defects cost review rounds: a prompt-note "also do X" outside the spec (#54 r1), an evidence-in-PR-body clause contradicting `help build` (#1756 r1), handles/PR numbers in AGENTS.md prose (#1757 amend), a count+order pairing rule that swaps classes (#1755 r1) and one-sided uniqueness (#1755 r2), a lexical `reads: o/` rule that flagged four legitimate inputs (#1754 bounce), a function named in the wrong file (#52). countermeasures filed: «oHqp_wJtT» (one mechanism; quote the grep hit; state both sides of a matching rule; docs-style for prose), «215t_A5ZL» landed (#54: ratchet rows named in the Change; commit before mutating).
- `gitboard spec` said "replaced" on an identical body (only meta changed) → a 12-min review round with no verdict; filed and built (#56, in review).
- pre-bootstrapping worktrees by hand cut fetch/build calls (specu: first edit at call 21 vs 33–40 cold; showrev 45 calls total) but not for research-shaped specs (ast node: first edit at 62); systematic form filed as `gitboard worktree` (in build). Its `--ref` must land after the bootstrap in refs/spike/ (FETCH_HEAD got overwritten once).
- `cosmic.ast` outcome ranked first at the goal owner's direction; Node+parse landed (#1758), walk/spans in build.
- costs by spec shape (builders, sonnet): exact-text/one-mechanism specs 27–45 calls; either/or or wrong-location specs 74–108; research pushed onto the builder 108–173. Reviews: 23–51 calls; docs-only with a trimmed brief 10.

## per-agent notes (condensed; numbers in the transcript table above)
- g0eF #44 build 32 calls: substring-extension mutation false negative → «VGEI» landed (#49). AY6h #45 85 calls: either/or mechanism; coverage gap diagnosed by grepping .cov → «coverage names uncovered lines» filed. QYqs #46 67 calls: clean. 4mpH #47 90 calls: flag needed gitcommands.tl (unstated) and brief_test.tl at 498. qPiX #1751 88 calls: three ratchet gates unnamed by the spec. eoMl #1752 98+34 calls: wrong index path + phantom contributing.md line + `reads: o/` failed cold CI → «reads lint» landed (#1754). iltX #1753 173 calls: 19-group cleanup; self-scanning fixture trap; cast-sites reconcile lost classes → «Iejv» landed (#1755, three rounds). duSw #48 92+32 calls: stray branch incident; argv parsing lived in gitboard.tl not gitshow.tl. VGEI #49 33, XBYz #50 27, 215t #54 30, kTNP #1756 30: exact specs. CBMu #1754 42+62: correct bounce, lexical rule. RwON #51 51. lWFs #52 86: predicate named in the wrong file; state literal not in schema; side-channel cache staleness. Rbqw #53 94+17: rev-parse walks up past .git-less dirs; reviewer found untested guard. wwWD #55 82: validation broke three fictional-path tests; brief_test.tl now 500/500. zAjL #1757 29+34. Iejv #1755 57+48+30. 0E11 #1758 108: 3-slot signature vs D20 r11; three cascading ratchets. AyZ4 #56 40+7: LCS surplus → bag count. T9sk #57 45: clean.
- reviewers: 23–51 calls each; recurring `cd /home` stumble (product_root fixed #55, not yet pinned); "diff against the base sha, not origin/main" and "warm bootstrap, never rm -rf o" removed all reviewer friction once in the env note → belong in the review template («x8CF»). Two reviewers re-ran the full gate against the brief; one docs-only review took 10 calls with a shape-specific brief.

## candidates
- filed this pass: «SmAQinD» main.tl headroom; «g0eF» (#44); «duSw» (#48); «QYqs» (#46); «hrqC» take --open; «x8CF» verdict lands; «VGEI» (#49); «castsdrift»; «XBYz» (#50); «covgaps»; «215t» (#54); «RwON» (#51); «sk32KU» (#57); «linked worktree guard» (#53); «readso» (#1754); «transpose»; «deadlock» (#52); «4QFJ_HSRc» residual; «oHqp_wJtT» bar sentences; «pin bump» (#1756); «worktree verb» (in build); «spec unchanged» (#56); «Iejv» respec (#1755).
- stays here: brief files must not live where an agent's cleanup can reach them — the `gitboard worktree`/`brief --out` pair should write the brief INTO the worktree (gitignored) or the tool should tell agents never to delete their brief's directory; the review template should say "remove only the worktree you created". A follow-up pin bump for #48/#55/#56/#57 once they are all merged.
- review AyZ4 round 2: accept, 20 calls (a 25-call budget in the brief held). The scratchpad deletion was the PR 57 reviewer's final cleanup (`rm -rf <scratchpad>` where its brief file lived) — "remove your review worktree" was read as "remove everything I touched"; the countermeasure above stands.

## build 8stN_P3q1 (sonnet) — PR cosmic-lua/work#58 (gitboard worktree verb), 2039s, 168 tool calls, 331k tokens
- the heaviest run of the pass. (1) It treated the orchestrator's mid-build SendMessage (the refs/spike amendment) as a prompt injection and ignored it — correct posture for an unverifiable channel, but the board spec HAD been amended, so the diff now disagrees with the spec; fix: amendments reach a builder through a channel it can verify (the PR itself, or "gitboard show ID — read-only — and compare") — a sentence for the builder brief; (2) my `<root>/../wt/<id8>` formula admitted two readings (~10 min) — a worked example belongs in the spec; (3) `git remote get-url` resolves insteadOf (~10 min fixture redesign); (4) two fixture anchor bugs (~5 min). Also touched gitboard.tl (dispatch) and brieftext_review.tl (placeholder) beyond the file list — necessary wiring, called out.
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/ac88892e0031aceba.output: events=644 tool_calls=168 wall=2038s
tokens: in=338 out=3920 cache_read=36261681 cache_create=294851
by tool: Bash=121 Edit=34 Read=7 ToolSearch=1 Write=4 mcp__github__create_pull_request=1
- 8stN builder refused the amendment a second time even with a read-only `gitboard show` verification path, because its brief forbids all gitboard commands — consistent, and a real gap: the brief has NO channel for a mid-build spec amendment. Countermeasure (brief text, part of «x8CF»/the brief items): "a spec amendment reaches you as a new brief; if the orchestrator resumes you with one, its full text is on the board and in the PR comment thread — act on the PR comment". Interim: a fresh builder with the amended brief as a rework (no new review round spent).

## build 4QFJ_HSRc (sonnet) — PR cosmic-lua/work#59 (deadlock residual), 1065s, 88 tool calls
- the spec (mine) said "an absent observation" where it meant "a row recorded against a different head"; the literal reading broke an existing gitverbs test (one full ci). The builder resolved it plausibly (recency applies only to stale-for-head rows, never to a never-observed PR) and flagged it for review. Spec lesson: name the boundary tests a change must preserve.
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a72753b73a708e949.output: events=346 tool_calls=88 wall=1066s
tokens: in=178 out=4234 cache_read=13193711 cache_create=177608
by tool: Bash=66 Edit=13 Read=7 ToolSearch=1 mcp__github__create_pull_request=1

## build gDNw_5bFk (sonnet) — PR cosmic#1759 (cosmic.ast walk + spans), 1460s, 96 tool calls
- the spec's `span_end(node)` signature cannot do the bracket check the same spec describes (the `expression_list` kind is used for both bracketed and bare lists); the builder threaded `Parsed.tokens` through and disclosed it (~20 min tracing tl.lua). A spec ported from a spike that held the source text did not carry the data-flow. Five throwaway probe scripts to learn field values (~10 calls) — a `--docs` example dumping one parsed tree's fields would pay for itself across the whole ast cluster. Coverage row needs one full run to learn the number (the «coverage names uncovered lines» item's sibling: print the row for a new file on the narrowed run too).
/tmp/claude-0/-home-user/f6f470de-dee2-5890-b8f1-db91217d1a73/tasks/a7ffac4528e263695.output: events=377 tool_calls=96 wall=1460s
tokens: in=194 out=3428 cache_read=15724767 cache_create=194798
by tool: Bash=82 Edit=8 Read=2 ToolSearch=1 Write=2 mcp__github__create_pull_request=1
- 8stN rework via a fresh brief: 43 calls / 6 min (vs the 168-call original): a rework brief with the amended spec verbatim is the working channel. "the brief wording naming the ref" was mine and vague — it meant the verb's help text.

## Reviewer — PR 59 (4QFJ_HSRc, deadlock residual) — accept
- 36 calls, ~96k tokens, 5.3 min wall. No friction reported.
- The brief's flagged ambiguity ("absent observation" scope) was resolved by
  reading spec grammar — a disclosed-ambiguity note in the review brief
  prevents a request-changes round. Countermeasure already filed (oHqp bar).
- Cost pattern: fresh review worktree bootstrap (`--make fetch`) remains ~8
  calls of the 36; the `gitboard worktree` verb (PR 58) removes it.

## Orchestrator — queue hygiene between reviews
- `next` recommended «5P4r_VUqR» (cosmic --find) at the queue head although
  its Ready-when names two items («omzs_ww5P», «SmAQinD») not yet done. The
  sentence was mine, buried in ## Evidence and not command-shaped, so the
  tool could not evaluate it. Cost: 3 orchestrator calls to notice + respec.
  Countermeasure: respec'd to a command-shaped Ready-when at the top of
  ## Change; filed `next` evaluating a "Ready when: `CMD` prints `OUT`"
  sentence and skipping not-ready items (see item below).
- `spec ID FILE --base` refused twice as "changed since you read it" because
  the base was cut from `show` output, which ends with an events line and
  the verdict line. The raw sidecar (`git show refs/heads/items/<id>:spec.md`)
  is the only clean base. 4 calls lost. «RSTv_DYmH» already names the fix
  ("give spec --base a clean source"); noted the exact pollution there.

## Reviewer — PR 58 (8stN_P3q1, gitboard worktree verb) — accept
- 31 calls, ~110k tokens, 4.5 min wall. No friction reported.
- Two files outside the spec's list (gitboard.tl dispatch, brieftext_review.tl
  placeholder) were disclosed in the PR body and accepted as wiring — the
  spec should have named them (oHqp bar: "quote the grep hit that places
  every function"); a dispatch table is always a second file for a new verb.
- Size overrun 213 vs "≤ 200" noted, not bounced. A soft line bound in a
  spec invites a judgment call each review; state either the house cap or
  nothing.
- Filed «XINc_troq» (Ready-when evaluated by next/take) this slot.

## Reviewer — PR 1759 (gDNw_5bFk, ast walk + spans) — request-changes
- 40 calls, ~140k tokens, 8.2 min wall. Real defect found: `walk` visits
  `OpInfo` tables (they carry `y`/`x` but no `kind`) — would corrupt the
  matcher built on it. Review earned its cost.
- Friction reported: (1) brief's verdict block says `cd /home` (product
  root wrong) while the env note under it corrects it — 1 failed call; the
  `product_root` fallback fix (#55) is merged but the pinned release
  predates it → pin bump now unblocks this. (2) mutation target choice cost
  a build+test cycle: nothing documents that tl's `xend` is the inclusive
  last column vs token `.x` start column; builder should note it in walk.tl.
  (3) warnings-as-errors made the naive mutation not compile — inherent.
- GitHub refused REQUEST_CHANGES review on own PR (same token) → comment
  fallback worked; «x8CF» (verdict bounces itself) removes the detour.

## Builder — PR 1759 rework (gDNw_5bFk, round 2) — pushed 4c386ae
- 61 calls, ~127k tokens, 8.4 min wall. Fix + test + doc line + cast-sites
  regen; CI green. Calls were high for a 3-hunk change: ~4 lost to
  `gitboard init` (below), the rest to the full `--make ci` and cast-sites
  regeneration that any walk.tl line shift forces («transpose» item covers
  same-line reconciliation).
- Friction reported: the agent ran `bin/gitboard init` in /home/user/cosmic
  (no GITBOARD_DIR exported) and got a dead local-only board, then found the
  skill's bootstrap by grep. My env note said "with GITBOARD_DIR=/home/user/work"
  instead of an `export` line the agent could paste. Countermeasure filed:
  bin/gitboard prefers a sibling `../work` clone whose origin is
  cosmic-lua/work before defaulting to o/board (the skill's shell snippet,
  moved into the trust root so no brief has to carry it).

## Orchestrator — pin bump «40VG_JWAD» done by hand (deliberate)
- A two-line pin edit built by the orchestrator in its own worktree: 4 calls
  (worktree + sed + verify + commit/push) vs ~35 for a builder agent. Review
  still goes to a fresh-context agent. First push rejected: author email set
  to the user's real address (GH007) — use the noreply identity the repo's
  history carries.
- Bar refusal on take: "spec reaches cosmic-lua/work with no declared read
  access — name it under ## Access". The release download counts as reach;
  `new` accepted the spec without saying so, `take` refused it — the bar
  should fail at `new` (or `show`) when it will fail at `take`.

## Reviewer — PR 1759 round 2 (gDNw_5bFk) — accept
- 25 calls, ~107k tokens, 4.4 min wall. No friction reported.
- The round-2 context section (what was found, what changed, what NOT to
  relitigate, the exact mutation to run) is what kept it at 25 calls vs 40
  in round 1. `brief review` should carry this itself when an item is in
  its second round («IB9Z_CwM4» bounce_context never fires — same root).
- Still ran the full `--make ci` in a fresh worktree (~8 calls) although CI
  was green on the head and the brief said CI is mechanical; the review
  brief should say "do not re-run the full gate" outright.

## Reviewer — PR 1760 (40VG_JWAD, pin bump) — accept
- 10 calls, ~70k tokens, 75 s wall. No friction.
- A review brief scoped as an explicit numbered script (read diff+checks,
  verify the sha, one mutation without a checkout, record) is 3–4x cheaper
  than the generic template for a mechanical diff. The ~70k tokens is
  almost all the brief + spec context; call count is the lever that moved.
- Countermeasure already filed as «xWSa_IIFM» (no full gate re-run); the
  bigger lever is a `brief review` shape keyed on diff size — a diff under
  ~10 lines gets the script form. Fold into xWSa's spec if it bounces.

## Orchestrator — pull of «oHqp_wJtT»
- `take` refused: the bare session already held 3IxvOg4f (the pin bump I
  built by hand under the session id rather than a `build-<handle>` label),
  and the rule is one claim per worker. 1 call lost; fixed by always passing
  `--session build-<handle>-<sid>` on every take, hand-built or not. The
  orchestrator's own hand builds must use a worker label too — note for
  `help orchestrate` when the hand-build exception is written down.
- Worktree + bootstrap by hand again (pin with `worktree ID` merges as PR
  1760); bootstrap ran in the background while the brief was filled, so no
  wall time was lost: take → spawn was 3 calls.

## Builder — omzs_ww5P (cosmic/ast matcher) — PR 1761
- 96 calls, ~232k tokens, 22.5 min wall. Largest deliverable of the pass
  (match.tl + 12-case test + 16 ratchet rows); no rework, all four
  acceptance counts matched the spec on first run.
- Friction reported: ratchet rows (cast classes, coverage pair) cannot be
  precomputed; the brief's "add them in the same edit pass" cost 2 extra
  gate-adjacent runs (ci fails on casts → reconcile; coverage fails → add
  row). Countermeasure: tell builders to run `_build/cast_sites.tl
  --reconcile` and `--make coverage` BEFORE the one full gate; systemic:
  a ratchet dry-run that prints every required row in one pass.

## Builder — oHqp_wJtT (bar sentences) — PR work#60
- 38 calls, ~96k tokens, 5 min wall. Handover recorded by the builder.
- Friction reported: ~5 calls lost to hand re-wrapping prose so a literal
  test substring survived a line break; builder's own care, no brief fix.
  Batching three sentences before the first test run cost one failing run.
- The matcher builder (PR 1761) did NOT record its handover (`take --pr`);
  the orchestrator did it after. Brief step says it; the agent's report
  listed the PR and stopped. «hrqC» (take --open) makes this moot: the board
  opens the PR and records the handover in one verb.

## Orchestrator — first pull under the new pin (hrqC)
- `take` refused with "verdicts before new work" (PR 1761 awaited a verdict);
  then refused claiming that review because 1761's CI was running (3/5) —
  both gates from #52/#59 live and correct. The right order was: claim the
  green PR's review (work#60), then the pull passed. 2 calls; the tool's
  messages named the next verb each time, so nothing was lost to guessing.
- `gitboard worktree 7zgt_hrqC`: 6.5 s, branch + worktree + bootstrap
  (warm fetch/build reuse) in one call, vs. 3 calls + a background job by
  hand. It ran without a claim — the verb does not check that `take`
  succeeded; harmless here, but the doctrine says "right after take
  succeeds" and the verb could refuse an unclaimed item (small item later).
- `brief builder` reports `<BRANCH>` as an unfilled placeholder: the spec
  quotes the template's own prose ("from `<BRANCH>`"), which matches the
  fill grammar. Fill grammar vs quoted prose — the bar should say to write
  such quotes without angle brackets, or brief should skip spans inside
  backticks.

## Reviewer — work#60 (oHqp_wJtT, bar sentences) — accept
- 8 calls, ~72k tokens, 75 s wall. No friction. Second review on the
  script-shaped brief; second time under 10 calls. The checkout-free
  mutation ("show FETCH_HEAD:<test>, confirm each substring sits on one
  source line") replaced a bootstrap+break+restore cycle (~15 calls) for a
  prose diff.
- Pattern to fold into «xWSa_IIFM»'s spec: for a prose/pin/config diff the
  review brief is a numbered script with a checkout-free mutation.

## Reviewer — PR 1761 (omzs_ww5P, ast matcher) — accept
- 28 calls, ~107k tokens, 4.2 min wall. No friction. Full review with a
  fresh worktree (code diff), no `--make ci` re-run — the env note's "do
  NOT re-run the full gate" held, and the calls went to re-measuring two
  acceptance counts through the diff's own API and a real mutation. This
  is the target shape for a code review: ~28 calls, not 40.

## Builder — PSmAQinD (parse_args → _cli/parse.tl) — PR 1762
- 65 calls, ~143k tokens, 11.5 min wall. main.tl 499 → 234. Handover
  recorded by the builder (env note now says "not optional").
- Friction reported: (1) 2 grep calls to confirm `Options`/`flags` were
  parse_args-only — the spec should have quoted that grep (oHqp's new bar
  sentence covers exactly this, landed today). (2) first mutation was
  behaviourally inert (a branch with an equivalent fallback) — 3 calls;
  builder brief step 5 could say "mutate an assignment target, not a
  branch with a plausible fallback". (3) a `_make/stamp.tl` BOOT_MODULES
  ratchet nobody named — caught by its test, cost one gate round; the
  spec's "the checker names each one" covered locals, not boot modules.
- Env note's ordering (build once → reconcile → coverage → one ci) held:
  one full gate, as intended.

## Reviewer — PR 1762 (PSmAQinD, parse_args move) — accept
- 29 calls, ~99k tokens, 4.2 min wall. No friction. The move was verified by
  diffing the old block against the new file (one re-anchored comment), plus
  a mutation on a flag the builder had not mutated.
- Label drift: the reviewer recorded under `review-PSmA_QinD-…` (the handle
  with its divider) while the claim was `review-PSmAQinD-…`; the verdict
  still landed, so the tool normalises — fine, but the brief's own "claim it
  as build-PSmA_QinD-…" vs the env note's label differ by a divider, and a
  stricter tool would refuse. Orchestrator: always use the tool's rendering.

## Builder — 7zgt_hrqC (gitboard take --open) — PR work#61
- 201 calls, ~296k tokens, 29 min wall — the pass's most expensive agent.
  Handover recorded. ~15 min of reading spent deciding whether to touch
  `gitverbs.tl`/`gitboard.tl` (dispatch wiring the spec did not list) —
  the SAME omission PR 58's reviewer flagged on the worktree verb. Rule for
  the bar: a new verb or flag ALWAYS lists `_work/gitboard.tl` (argv) and
  `_work/gitverbs.tl` (dispatch) — file as a bar sentence + `help take`.
  The Evidence cited `--pr N` logic in gittake.tl when it lives in
  gitverbs.tl's cmd_take: a wrong-location citation, the exact defect
  oHqp's new "quote the grep hit" sentence targets (landed after this spec
  was written).
- Other losses: a fake transport typed as `any` (one rejected type check,
  ~10 min); an Edit that matched a non-unique anchor and deleted a
  function's `return`/`end` (~5 min); one gitboard call from the wrong cwd
  (harmless). Countermeasure for the seam: spec says "the fake is typed
  against cosmic.fetch's Options/Response/Error".

## Reviewer — work#61 (7zgt_hrqC, take --open) — request-changes
- 52 calls, ~138k tokens, 9.7 min wall. Real finding: the `--pr`-conflict
  test shares a no-repo fixture, so the next refusal masks the guard — a
  dead negative assertion («M2oa_n7au»'s class). The env note's steer
  ("mutate a local refusal, not the 422 needle the builder covered") found
  it; an unguided mutation would likely have repeated the builder's.
- Friction reported: 4 calls lost diffing from the API's `base.sha` (base
  tip, not merge-base). Env note: "trust get_files; if you diff locally,
  diff from `git merge-base HEAD origin/main`".
- Non-blocking: a third inline copy of the master/main base fallback where
  `gitworktree.default_base` exists — the builder read the dispatcher for
  15 min but not the sibling verb's helper; the bar's grep sentence applies.

## Builder — PR work#61 rework (7zgt_hrqC, round 2) — pushed 639bcc68
- 48 calls, ~99k tokens, 5.4 min wall. Both findings closed; mutation run
  both ways; handover recorded. One gap: the reviewer said `default_base`
  "already exists" but it was a private local — 2 calls to export it.
  Reviewer wording rule: say "exported" when you mean callable.

## Builder — 5P4r_VUqR (cosmic --find), round 1 — STOPPED at the gate
- 109 calls, ~211k tokens, 18 min wall; feature built and verified end to
  end, then `--check lint` (visibility) refused `require("cosmic.ast.*")`
  from `_cli/`: `cosmic/ast/` has no public parent `init.tl`. The builder
  stopped per the brief (correct), diff committed unpushed on the branch.
- Root cause: three library items (node, walk, match) landed with no
  outside consumer, so the missing public parent was invisible to their
  reviews; the --find Ready-when checked "completed", not "consumable".
  Countermeasures: (1) spec amended (+init.tl, select_files named, M
  defined) and the same worktree resumed — no rebuild; (2) filed a lint:
  a `cosmic/<name>/` directory with shards and no `init.tl` is a finding.
- Other: ~6 reads to find "the way --check walks them" = `_make.check.
  select_files` (name the function, not the verb); coverage `total` moved
  between two runs (in-process test added), so "one coverage run" is
  optimistic — say "run coverage after the last test edit".

## Reviewer — work#61 round 2 (7zgt_hrqC) — accept
- 31 calls, ~111k tokens, 5.2 min wall. No friction. Round-2 context with
  the exact mutation and "do not relitigate" cut it from 52 to 31 calls.

## Orchestrator — pull of «J0zb_occu»
- `take` refused on the bar: the fixture's origin URL string counted as
  reaching cosmic-lua/work, needing `## Access` — second time this pass
  («40VG_JWAD» too). `new` accepted both specs silently; the bar problem
  only surfaced at `take`. Countermeasure: `new --spec-file` prints the
  same bar problems `take` would (or refuses) — file under c70t_COiw.
- `gitboard worktree` ran and bootstrapped for an unclaimed item again
  (3 s wasted, harmless). Same note as before: the verb should refuse
  without the caller's claim.

## Builder — 5P4r_VUqR (cosmic --find), round 2 resume — PR 1763
- 47 calls, ~117k tokens, 6.4 min wall. Resuming on the stopped commit
  cost 47 calls vs ~110 for a fresh build; the amended spec + "start from
  commit X, what remains is…" note is the pattern for any stop-and-report.
- Friction reported: a new public module also trips
  `_build/public_surface_baseline.tl` (one extra coverage run). Rule for
  briefs: "a new `cosmic.<name>` public module: run
  `--make run _build/public_surface.tl --baseline`" — belongs in the
  builder template's ratchet list, not per-brief prose.

## Orchestrator — PR 1763 CI red (example ratchet), fixed by hand
- The `ci` lane failed on `test_every_public_module_has_example_or_waiver`:
  a new public module (`cosmic.ast`) must ship an `init_example.tl`. The
  builder's local `--make ci` was green — the example ratchet only runs in
  the coverage stage's instrumented pass in CI's environment, so the
  builder could not have seen it. Fixed by hand: 6 calls (write, fix
  unused-warning, build, example+test, full ci, commit+push) vs ~40 for a
  rework agent.
- Third ratchet a new public module trips (coverage row, public_surface
  baseline, example): the builder template's ratchet list should name all
  three under "a new `cosmic.<name>` public module".

## Orchestrator — PR 1763 CI red a second time (doc-returns ratchet)
- Head 0ead6aa failed `_build/doc_returns_test`: `span_start`/`span_end`
  carry one combined `@return integer, integer` tag for two slots — invisible
  while `cosmic.ast` was internal, a finding the moment it went public.
  Fixed by hand (5 calls). Two CI rounds, ~10 min each, for two ratchets
  the builder's local `--make ci` did NOT report — the local gate was green
  both times while CI was red: the `_build/*` doc ratchets run over the doc
  index, which the converged local build appears to have served stale
  (public-module set from the previous build). Worth its own item: "local
  --make ci green, CI red on doc ratchets — the doc index the _build tests
  read is not rebuilt when a module becomes public". Filed below.
- Both fixes were correct-by-inspection and cheap by hand; a rework agent
  would have cost ~40 calls each.

## Builder — J0zb_occu (bin/gitboard sibling board) — PR 1764
- 86 calls, ~152k tokens, 11.4 min wall for a ~15-line sh change + 60-line
  test. ~6 calls lost to a stale test-result cache: the test reads
  `bin/gitboard` at runtime (not a require), so editing it did not
  invalidate the recorded result. The mechanism exists — a test declares
  `--- reads: <path>` (PR 1752 uses it for the doc index) — but nothing in
  AGENTS.md's Testing section says so. Countermeasure: one sentence there.

## Orchestrator — «eoMl_RZUo» PR 1752 left in rework since 16:11
- My own bookkeeping error: the pre-compaction summary recorded eoMl as
  merged and I removed its worktree; the board had it in rework with one
  small finding (a docs sentence overclaiming doc_paths_test's roots).
  3.5 hours idle. `gitboard show`'s doing list is the truth — read it at
  every reconcile, not memory. Recreated the worktree; fixing by hand.
- eoMl rework done by hand: 1 sentence in docs/contributing.md, 5 calls
  (edit, gate, commit+push, handover, comment) after a ~3 min background
  bootstrap. Round-2 review pending.
- PR 1752 CI red on the rework head: `_build/prose_dupes_test`, a ratchet
  that landed on main after the branch was cut (the local gate warned
  "HEAD is not a descendant of origin/main … CI judges the merge"). Merged
  main in, reworded one comment, 4 calls. Rule for hand fixes on an old
  branch: merge origin/main BEFORE the local gate, so the gate judges what
  CI will.

## Reviewer — PR 1763 (5P4r_VUqR, cosmic --find) — accept
- 38 calls, ~119k tokens, 5.1 min wall. Re-measured all four acceptance
  shapes through the built binary; mutation on the sort order (steered
  away from the builder's exit-code mutation). No findings.
- Friction reported: the spec's Ready-when quoted a 7-char handle
  (`SmAQinD`, missing its leading P) — my typo from the spec write — cost
  4 calls. A Ready-when command that runs at `next`/`take` («XINc_troq»)
  would have refused the typo at filing time; until then, spec authors
  paste handles from `show` output, never retype.

## Reviewer — PR 1764 (J0zb_occu, sibling board) — request-changes
- 19 calls, ~88k tokens, 5.1 min wall. Real bug: under `set -e`, a missing
  `../work` makes `sibling="$(cd … && cd work)"` abort the script silently
  before the o/board fallback — the no-sibling case the spec names. The
  PR's own test covered "sibling with wrong origin", never "no sibling".
  Second finding: the missing `--- reads: bin/gitboard` line («eoxA_GRW9»
  filed on this) — the reviewer's mutation reused a stale cached pass.
- Friction reported: `FETCH_HEAD` clobbered by a concurrent reviewer's
  fetch in the shared /home/user/cosmic clone — 2 calls. Env note fix: pass
  the head sha to `git worktree add`, never FETCH_HEAD.
- PR 1764 rework by hand: 6 calls (edit script+test, run, mutation both
  ways, gate, commit+push, handover). The no-sibling case is the one the
  spec named first and the builder never tested — a spec that lists the
  test cases by name would have included it; mine listed three and the
  builder wrote exactly those three.

## Orchestrator — --find landed; first use
- «5P4r_VUqR» done (PR 1763 merged). From main: `o/bin/cosmic --find
  'check.must($X)' _cli` → `find: 83 hit(s) in 48 file(s)` in one call —
  the measured fact a sweep spec quotes, no builder reading N sites.
- Pulled «FsQs_WryR» (rewrite/splice) with the worktree verb (1 call) and
  a spike-ref fetch by hand (`worktree --ref` would have done it — the
  orchestrator forgot the flag; 1 call). Env note names the four landed
  ast modules the spec predates, and the three public-surface ratchets a
  widened `cosmic.ast` trips — the two things that cost the --find builder
  its stop and two CI rounds.

## Reviewer — PR 1752 round 2 (eoMl_RZUo) — accept
- 29 calls, ~99k tokens, 5 min wall. No friction; ~8 of the calls were the
  fresh-worktree bootstrap the mutation needed (a doc-index test cannot be
  checked without a build). Round-2 context kept it to one mutation.

## Reviewer — PR 1764 round 2 (J0zb_occu) — accept
- 11 calls, ~84k tokens, 2.2 min wall. No friction. Checkout-free
  reproduction as the mutation: three fixture roots from `git show
  <head>:bin/gitboard`, no build. Observation filed: SSH origins
  (`git@github.com:cosmic-lua/work.git`) do not match the `*/cosmic-lua/work`
  suffix — «below».

## Orchestrator — J0zb landed (PR 1764 merged)
- Verified from /home/user/cosmic with no `GITBOARD_DIR` exported:
  `bin/gitboard sync` → "state is current" against the sibling /home/user/work.
  The export line leaves every future brief; the `gitboard init` wrong turn
  (PR 1759 rework, 4 calls) cannot recur from this checkout.
- Two builders in flight (FsQs splice, x8CF verdict-lands-PR); no reviews
  pending. Items done this pass so far: 4QFJ, 8stN, gDNw, 40VG, omzs,
  oHqp, PSmAQinD, hrqC, 5P4r, eoMl, J0zb (11); PRs merged: work #58 #59
  #60 #61, cosmic #1759 #1760 #1761 #1762 #1763 #1752 #1764 (11).

## Builder — FsQs_WryR (ast rewrite/splice) — PR 1765
- 103 calls, ~188k tokens, 15 min wall. Handover recorded; both acceptance
  fixtures; mutation on the refusal guard. Found a real bug in MY example
  (`init_example.tl` reads `captures["CMD"]`; keys are the desugared token
  `"_CMD"`, so the print never ran — the example ratchet checks that an
  example runs, not what it prints). Fold the fix + a doc line in match.tl
  ("capture keys carry the desugared `_` prefix") into «5ass_BX1Y»'s spec,
  which touches the example anyway.
- Friction reported: ~5 calls on the capture-key assumption (trusting the
  wrong example); ~4 calls on two ratchets the env note's gate list did not
  name (prose_dupes, the gsub-marker lint) — both self-explanatory at the
  one full gate, so the list stays as is; 1 call on `tl.Token.comments`
  typed non-optional but nil at runtime (a `.d.tl` note).

## Reviewer — PR 1765 (FsQs_WryR, ast rewrite) — request-changes
- 32 calls, ~117k tokens, 6.1 min wall. Real bug: unanchored `gsub`/`find`
  on desugared capture names, so `$X` rewrites inside `$XS` — silent, valid
  output the formatter check cannot catch. Reproduced live. Also: both
  shipped tests are single-hit, so the back-to-front order mutation passed
  them; the reviewer wrote a two-hit fixture (the env note said to).
- Friction reported: the generic "the diff's own test catches it" sentence
  vs the env note's "write a two-hit fixture" read as a conflict; fold into
  «xWSa_IIFM»: "a scratch fixture is an acceptable mutation witness when the
  diff's tests cannot see the mutation — that gap is itself a finding".

## Builder — fQ71_x8CF (verdict lands the PR) — PR work#62
- 146 calls, ~270k tokens, 23 min wall. Handover recorded. New
  `_work/ghwrite.tl` (the 500-cap split the env note pre-authorised).
- Friction reported: (1) MY env note named `_work/gitverbs.tl` as holding
  `cmd_verdict` — it lives in `gitverdict.tl`; a wrong-location claim from
  the orchestrator, the exact defect the bar's grep sentence targets (1
  grep, no edit). (2) "`--body` required" read unconditional; ~12 calls to
  learn it must gate on a real fetched PR (20+ fixture call sites have no
  origin) — the spec should say "gate as accept's merge-check does".
  (3) `take --pr` itself round-trips GitHub (`handover_refusal`), so a
  verdict test fixture must stub the transport before the take — 1 failed
  run + a helper rewrite. (4) `fetch.Error` is a record, not a string — 1
  type-check cycle.

## Builder — PR 1765 rework (FsQs_WryR, round 2) — pushed 6eebd10
- 32 calls, ~109k tokens, 5.7 min wall. Whole-identifier `find_name` shared
  by substitution and `kept_ranges`; both requested tests; mutation both
  ways; handover recorded. One extra gate round: the nil-returns ratchet
  refused an `integer, integer` signature that returns nil — the honest
  `T | nil` shape the AGENTS.md convention already states.

## Reviewer — work#62 (fQ71_x8CF, verdict lands the PR) — accept
- 31 calls, ~128k tokens, 5.3 min wall. No friction. Both disclosed
  deviations judged with evidence (the 429-line gh.tl, the six no-origin
  fixture sites); mutation on the sha-pinned merge PUT. Follow-up noted:
  end «IB9Z_CwM4» not-planned when #62 merges (the spec says so).

## Reviewer — PR 1765 round 2 (FsQs_WryR) — accept
- 26 calls, ~107k tokens, 3.7 min wall. No friction. Both reproductions
  re-run from a scratch script; the round-1 mutation repeated; one shared
  matcher confirmed by grep.

## Orchestrator — x8CF landed (work#62 merged)
- «fQ71_x8CF» done; «IB9Z_CwM4» ended not-planned as its spec directed.
  With #58 (worktree), #61 (take --open) and #62 (verdict lands the PR)
  on work's main, a builder and a reviewer touch no GitHub tool; the next
  pin bump makes it live for this checkout. Pulled «XINc_troq» with the
  verb: 1 call (take + worktree + brief), env note names gitview's 10-line
  headroom and the fixture rule.

## Orchestrator — FsQs landed; --rewrite pulled
- «FsQs_WryR» done (PR 1765 merged, 2 rounds). The cosmic.ast chain the
  user prioritised is on main end to end: node → walk → match → find →
  rewrite; `--rewrite --fix` is the last CLI piece, building now.
- The «5ass_BX1Y» spec predated `--find` and asked for a second search
  implementation with a different line shape; amended before the pull so
  search mode reuses `_cli/find.tl` (one mechanism) and named the three
  wiring sites by line. Cost: 2 calls; saves a duplicate module and the
  review round that would have caught it.

## Builder — XINc_troq (Ready-when gate) — PR work#63
- 147 calls, ~259k tokens, 22 min wall. Handover recorded. New
  `_work/gitreadywhen.tl` (gitview at 490 — the env note's split, taken).
- Friction reported: (1) a second, uncited doctrine test asserted the old
  wording — 2 calls; the spec should quote the grep for the SENTENCE, not
  a line range. (2) two sibling leaves tie in the queue and the session-
  seeded tie-break reshuffles them — one failing full-gate run; fixture
  helper does not rank siblings — a note for `_work/fixture.tl`'s doc.
  (3) no sibling uses `cosmic.child`'s timeout path — 4 doc calls, genuine
  research.

## Orchestrator — pin bump «Vm86_JZLA» by hand, round 2
- `take` refused ("verdicts before new work" — #63 awaited a verdict) but
  the orchestrator ran `worktree`, edited and pushed anyway before reading
  the line: the branch is fine, the claim was missing. «9doI_nexi»
  (worktree refuses an unclaimed item) is exactly this. Claimed the #63
  review first, then the item.

## Builder — 5ass_BX1Y (cosmic --rewrite) — PR 1766
- 117 calls, ~220k tokens, 21 min wall. Handover recorded; search mode
  byte-identical to --find (tested); example fix + match doc line in.
- Friction reported: (1) `--fix` collides with the existing `--format`/
  `--fix FILE` flag — `cosmic.flags` refuses duplicates, so the builder
  overloaded it and guarded main.tl's fix dispatch: ~15 calls; a design
  smell the reviewer must judge (an `--apply` or `--rewrite-fix` name
  would avoid it) — my amended spec kept the spike's `--fix` without
  checking the flag table. (2) my env note named a nonexistent
  `init_example_test.tl` — 1 call. (3) the dupes ratchet is per function
  body, so `files_for` had to be exported from find.tl — one coverage run.
  (4) the spec cites `docs/design/ast-rewrite/README.md`, which is not in
  the tree (spike-only) — dead reference in an old spec.

## Orchestrator — ast applications filed from measured counts
- Surveyed with `cosmic --find` (7 patterns, one call each): error 47,
  os.exit 49, assert 6835, cosmo.unix in tests/examples 4; cast-sites.tsv
  regenerated 12 times in its history, 4 today for line shifts alone.
- Filed «G3Mu_4ITj» (cast sites keyed by AST node — kills the reconcile
  tax on every PR), «npTS_YYtP» (markers as an AST rule; throws:/exits:
  gated, baseline for the 96 unmarked sites), «KnTF_wcsP» (bar sentence
  unconditional + reviewer re-runs the pattern on the PR head). Ranked
  first under the ast outcome / after the ratchet-brief item.

## Reviewer — work#63 (XINc_troq, Ready-when gate) — accept
- 34 calls, ~105k tokens, 4 min wall. No friction. Mutation on the
  two-span guard (the env note's steer). Noted: the spec named
  `cosmic.proc` for spawning; the spawn API is `cosmic.child` — a wrong
  module name in MY spec, harmless here because the builder read the docs.

## Reviewer — PR 1767 (Vm86_JZLA, pin bump) — accept
- 10 calls, ~73k tokens, 73 s wall. No friction. Third script-shaped review
  at ≤10 calls («QaZ8_CyOn» makes the shape the tool's).

## Orchestrator — filings on the goal owner's direction
- Filed the six ast applications («G3Mu», «npTS», «KnTF», «aAVJ», «OsLB»,
  + _make/imports) and a new outcome «ineK_wWK6» "gates assert their
  expectation from the command line, not from committed tracking files"
  with two children (coverage --min/--min-file replacing .cosmic-coverage;
  --max-lines replacing the 500 constant), each amending its decision
  record (D06/D27/D39) through the decide skill. Ranked the outcome
  after G8. `new --last` is not a flag (it is `rank`'s) — 1 call lost.

## Orchestrator — first `take --open` (YDqx_N2FC by hand)
- Two own wrong turns: (1) `take … | tail -1 && worktree …` laundered the
  refusal's exit 1 through the pipe — the exact AGENTS.md warning — so the
  worktree ran for an unclaimed item again; (2) the commit ran in the
  main checkout, not the worktree (a `cd` at the top of the chain), so the
  pushed branch had no diff and `--open` got a bare
  "HTTP 422: Validation Failed". Countermeasure for the tool: `--open`
  surfaces GitHub's `errors[].message` ("No commits between main and
  3Iy8ljyJ") on the refusal line — file under c70t. For the orchestrator:
  never pipe a gitboard verb before `&&`.

## Reviewer — PR 1766 (5ass_BX1Y, cosmic --rewrite) — request-changes
- 48 calls, ~134k tokens, 8.9 min wall. Real bug, reproduced: the
  overloaded `--fix` makes arguments positional across two lists; a mixed
  order silently reports `0 applied`. The spec (mine, from the spike) kept
  `--fix` without checking the flag table — «quIU»'s wiring-files sentence
  would not have caught a NAME collision; the bar needs "a new flag's name
  is checked against `_cli/args.tl` at spec time". Reviewer used
  `verdict --body` (new pin) — the first review with no GitHub tool.
- Friction reported: the env note's mutation would not compile (guard is
  type-enforced) — 3 calls; "if it does not compile, that is the finding".

## Orchestrator — first `take --open`: PR 1768 opened by the board
- Run from the product root only: from a worktree, `bin/gitboard` resolves
  `<worktree>/o/board` (no sibling `../work` there) — "not a git
  repository". The brief says so; the orchestrator forgot. The verb worked
  first time from /home/user/cosmic: branch → PR → handover, one call.

## Builder — Kk0N_r6Dd (builder brief ratchets) — branch 3IyFMFjW
- 35 calls, ~110k tokens, 4.5 min wall. No friction. First builder under the
  new step 8: pushed the branch and handed back a PR paragraph; the
  orchestrator opened the PR with `take --open --body` in one call.

## Reviewer — PR 1768 (YDqx_N2FC, SSH origin) — request-changes, NOT recorded
- 19 calls, ~84k tokens, 3.4 min wall. Real defect in the orchestrator's
  hand fix: dropping the slash admits `evilcosmic-lua/work`; fix enumerates
  `/` and `:` separators. Fixed by hand (+1 negative test), pushed.
- Tool gap, blocking: `verdict request-changes` → HTTP 422 on a PR that
  `take --open` opened (same bot identity; GitHub refuses REQUEST_CHANGES
  on one's own PR). PR 1766's bounce worked only because a builder opened
  it under the connector's identity. Filed as the top item under c70t; a
  bare "422 Unprocessable Entity" hid the cause («U8Qv_yhHw» surfaces it).
- `show` prints no CI line although the review brief says to read it
  there — 2 calls; filed.

## Reviewer — work#64 (Kk0N_r6Dd, brief ratchets) — accept, NOT recorded
- 19 calls, ~88k tokens, 3.4 min wall. Judged accept; `verdict accept`
  REFUSED twice: merge PUT refused by the merge queue → GraphQL auto-merge
  → this environment's proxy pins GraphQL operations → 403 → the verb
  dropped the verdict. Orchestrator re-recorded with `--no-land` under the
  reviewer's label and armed auto-merge through the connector. Filed as
  the top item under c70t: record first, land second. Until it lands,
  reviewers run `accept --no-land` and the orchestrator lands.
- Also: `show`'s `absent:` probe checks the board checkout's OWN tree (a
  work-repo path from /home/user/cosmic reads as absent) — 2 calls; the
  probe should run against the item's repo checkout. Same `show` CI-line
  gap as PR 1768's reviewer (filed «ZD1x_zeG6»).

## Reviewer — PR 1768 round 2 (YDqx_N2FC) — accept (--no-land)
- 6 calls, ~69k tokens, 46 s wall. No friction. The cheapest review of the
  pass: round-2 context + a one-line reproduction table + no checkout. The
  orchestrator armed auto-merge (1 connector call) until «eWXR_dtJc»
  lands.

## Builder — PR 1766 rework (5ass_BX1Y, --apply) — pushed 1cf6fe8
- 87 calls, ~172k tokens, 14.4 min wall. Found and fixed a second instance
  of the same mechanism (`--apply` missing from the greedy-flag set) by
  reproducing the reviewer's commands before writing tests. One cast-sites
  line-shift round («G3Mu_4ITj» removes it). The greedy pre-scan that must
  know every flag is a design tax `_cli/parse.tl` carries — worth its own
  item once --rewrite lands.

## Builder — xWSa_IIFM (review brief: no full gate) — branch 3Ixx9yIJ
- 32 calls, ~91k tokens, 4.8 min wall. No friction. PR opened by the
  orchestrator with `take --open --body` (1 call).

## Orchestrator — reviews-before-pulls, second time
- `take quIU` refused while #65 awaited a verdict; claiming #65's review
  cleared it and the pull passed — 2 calls, the tool's message named the
  verb. `brief review` on #65 left `<HEAD_SHA>` unfilled (the board's cache
  had not observed the head yet, 30 s after the PR opened); filled by hand
  from the builder's report — the brief should read the head from the PR
  payload when the observation is absent («ZD1x» adjacent; note there).

## Reviewer — work#65 (xWSa_IIFM, no full gate) — accept (--no-land)
- 6 calls, ~75k tokens, 79 s wall. No friction. Script-shaped, checkout-
  free. Auto-merge armed by the orchestrator (1 connector call).

## Reviewer — PR 1766 round 2 (5ass_BX1Y, --apply) — accept (--no-land)
- 54 calls, ~133k tokens, 6.6 min wall. Reproduced the round-1 bug against
  `--apply`, ran the greedy-set mutation both ways. ~3 calls on the
  brief's "CI is read from show" line («ZD1x_zeG6» filed).
- The last piece of the ast chain the user prioritised: node, walk, match,
  --find, rewrite, --rewrite --apply — all accepted; --rewrite merging now.

## Orchestrator — pulled «G3Mu_4ITj» (cast sites by AST node)
- take → worktree → brief → spawn in 2 calls under the new pin. First
  ast-application item to build; the env note names the acceptance (a
  blank line above a cast changes nothing) so the builder measures the
  claim the item exists for.

## Builder — quIU_cHds (wiring-files bar sentence) — branch 3Iy21s8C
- 30 calls, ~87k tokens, 3 min wall. No friction. Noted: `_work/doctrine.tl`
  is at 493/500 — the next doctrine sentence needs a split (a `bar` topic
  file of its own); flag on «KnTF_wcsP» before it pulls.

## Orchestrator — PR 1766 blocked by its own round-1 review
- After the round-2 accept, GitHub kept the round-1 REQUEST_CHANGES
  review standing (`mergeable_state: blocked`); the board records the
  accept but dismisses nothing. Dismissed by hand with one REST PUT under
  the board's token; filed «below» so `accept` dismisses the token's own
  standing request-changes before landing. Third landing gap found today
  in the new verdict verb (GraphQL 403, self-review 422, standing review).
- The by-hand dismissal was refused by the session proxy (403, "not
  permitted for this session type"): PR 1766 needs a human to dismiss
  review 5126646579, or «Qa2H_5whT» to land and a fresh accept. Noted on
  «2FvN_J32f». Every GitHub reach a remote session cannot make is a case
  for the board doing it from a local session — or for COMMENT reviews.

## Reviewer — work#66 (quIU_cHds, wiring-files sentence) — accept (--no-land)
- 5 calls, ~70k tokens, 46 s wall. No friction. Prose reviews under the
  script shape now run 5–6 calls; the floor is the brief's own size.

## Orchestrator — work#65 merge conflict, resolved by hand
- #64 and #65 both appended tests at the end of `_work/brieftext_test.tl`;
  #65 went `dirty` once #64 merged (auto-merge never enqueued it, and no
  conflict notice arrived). Merged main in, kept both sides, one `end` the
  naive marker strip dropped (1 extra call), gate green, pushed: 4 calls.
  The bar's "file-disjoint siblings" rule was known; the orchestrator ran
  the two brief items in parallel anyway because they edit different
  TEMPLATES — but they share one test file. Rule to carry: the test file is
  part of the footprint; sequence items that share it.
- The judged head moved (415e93aa → b4a75c7c merge commit); if `done`
  refuses on the moved head, a 5-call script review on the merge commit.

## Builder — 0Ked_J5tg (shards-without-init lint) — branch 3Iy4LaA1
- 64 calls, ~154k tokens, 10.3 min wall. ~4 calls: my env note named
  `_cli/visibility_test.tl` as existing (it is new) — the spec should say
  "(new)" on every file it names that is not in the tree; the `absent:`
  probe line already knows. ~6 calls: the lint runs per file, so a
  directory-level check needed a design read — the env note's one-level
  `ls -d cosmic/*/` command disambiguated it. Out of scope noted:
  `cosmic/quicksand/proxy/` has shards and no init.tl one level deeper.

## reviewer 0Ked_J5tg (PR 1769, shards-without-init lint) — full review, fresh worktree
numbers: tool_calls=36 (all Bash) wall=311s cache_read=2.85M cache_create=59k out=397; errors 0; repeats 1 (the test file run twice: pre- and post-mutation — legitimate)
verdict: accept --no-land, recorded first try; auto-merge armed by orchestrator via MCP.
agent's account: none.
observation: 36 calls for a full review is the floor for the fresh-worktree shape — bootstrap (fetch+build) alone is ~8 calls and ~2 min of the 5. the checkout-free script shape stays 5–11. no countermeasure beyond QaZ8 (script-template review brief) already in build.

## builder G3Mu_4ITj (cast sites keyed by AST node) — big _build change, PR 1770
numbers: tool_calls=107 (Bash 89, Read 6, Edit 4, Write 3, Monitor 4) wall=1578s (26 min) cache_read=19.2M cache_create=202k out=2.4k; first edit at call 32; errors 4; repeats 5 (cast_sites.tl read 5x, its test 3x).
agent's account (5 entries), sized:
- brief's AST shape for casts was wrong (said kind=="cast" with e1/casttype; real shape is an "op" node with op.op=="as", the cast child in e2) — ~4 probe scripts, ~10 min. countermeasure: the spec's evidence for an ast-shaped change must be a pasted `cosmic --find`/`bin/cosmic _tool/…` dump of the real node, not a description; filed as part of «npTS_YYtP»-style guidance → new item below.
- multi-cast-per-line files (3 files) made the AST count 138 vs lexer 134 — one probe round. countermeasure: env note for future cast items.
- `--check types` against a stale sibling snapshot — one round; already in AGENTS.md, agent did not read it in time. (recurring: 3rd agent this pass. countermeasure: the builder brief's environment block should carry the one-line "`--make build` before `--check types` on an edited sibling" — file as a brief-text item.)
- self-inflicted O(n²) test (scan per row) → 200s monitor timeout, kill, ~5 min, ~6 calls. no brief countermeasure.
- schema migration unstated in spec: the first reconcile after a schema change can't bootstrap; agent wrote a bridge script. countermeasure: a spec that changes a committed tracking file's schema must say how the existing rows migrate.
orchestrator-side: the item's spec said nothing about `docs/design/casts.md`, which now describes the old scheme — a stale doc no gate catches; filed as a follow-up item.

## orchestrator (pulling the PR 1770 review)
- `gitboard brief review ID --session …` → "unknown option: --session" — brief already fills the session label from the claim; one wasted call and an empty prompt file. countermeasure: none needed; help text is clear, my hand was on autopilot from `take`.
- `brief review` still leaves `<HEAD_SHA>`, `<PR_NUMBER>`, `<PR_URL>` for sed right after `take --open` (the board has all three by then) — 2nd occurrence this pass; item «ZD1x_zeG6» covers it.

## reviewer G3Mu_4ITj (PR 1770, cast sites by AST node) — full review, fresh worktree
numbers: tool_calls=46 (Bash 44, Edit 1, Read 1) wall=311s cache_read=4.2M cache_create=85k out=393; errors 0; repeats 0.
verdict: accept --no-land first try; auto-merge armed by orchestrator. agent's account: none.
observation: 46 calls, 0 errors, 0 repeats — the env block's explicit verify list (5 numbered checks + a named mutation target + "known, do not bounce" for the stale doc) is what keeps a big-diff review at one round. that block is hand-written every time; item «QaZ8_CyOn» (in build) templates it.

## builder QaZ8_CyOn (script-shaped review template) — work repo, PR 67
numbers: tool_calls=92 (Bash 55, Edit 20, Read 16, Write 1) wall=940s (16 min) cache_read=13.8M cache_create=174k out=1.1k; first edit at call 31; errors 0; repeats 8 (brieftext_review.tl read 10x, brief_test.tl 9x, brief.tl 7x — re-reading after each edit instead of holding the file).
agent's account (2 entries), sized:
- my env block measured brieftext_review.tl (224) and brief.tl (411) but not `_work/brief_test.tl`, which the Change names and which sat at exactly 500 — ~3 edit/measure cycles, a mid-task split into a new test file. countermeasure: the orchestrator measures EVERY file the Change names, and treats "at cap" as "cannot fit"; the builder brief's step 1 already says so — the miss was mine. (applied to OsLB's env block: all four files measured, placement decided up front.)
- a new field on a shared record (`Pull.additions/deletions`) is nil in every hand-built fixture literal; caught by reasoning, not by the checker. countermeasure: none from the brief; a general Teal gotcha worth a `guide.gotchas` line — filed below as a doc item candidate only if it recurs.
orchestrator-side: the change grew `gh.get_files` and two `Pull` fields the spec did not list — disclosed in the PR body; the reviewer judges it.

## orchestrator (npTS_YYtP pulled then ended not-planned)
- I filed npTS's spec myself earlier from `--find` counts and the `_tool/lint.tl` header, without grepping `_cli/` for an existing rule: `_cli/throw_lint.tl` (since #1681) already gates `-- throws:`/`-- exits:` token-exactly. cost: take + `worktree` bootstrap (~2 min wall) + 4 investigation calls + drop/done/spec dance (5 calls); saved: a builder round (~30–100 calls) that would have stopped at step 1. countermeasure: before spawning any builder, re-run the spec's `## Evidence` claims that assert an ABSENCE ("nothing gates", "no test", "no rule") with a grep over `_cli/ _build/ _tool/` — now a standing step in this pass; a bar sentence for absence claims filed as an item below.
- `done --reason not-planned` on a claimed, PR-less item needs `--force --why`; `drop` needs `--why`; `spec` on the ended item refused twice (claimed → --force; then "--base FILE" → then "changed since you read it" though I read it via `show` seconds before — `show`'s render is not the sidecar's bytes). 3 refusals, the disproof never landed on the board; it lives only here. countermeasure item: `spec --base` should accept what `show` prints, or a `show --raw` should exist.

## reviewer QaZ8_CyOn (work PR 67, script review template) — full review, fresh worktree
numbers: tool_calls=48 wall=297s cache_read≈? out≈? (see friction.tl line above in the transcript log); errors: plain `verdict accept` refused (GraphQL 403) then re-run with `--no-land` — the env block said to add it, the agent tried plain first: +1 call. also called `mcp__github__pull_request_read get_check_runs` despite "you touch no GitHub tool" — harmless read, but the brief's rule was ignored; the brief's own text says CI is read from `gitboard show`.
verdict: accept --no-land; auto-merge armed by orchestrator. agent's account: none.
countermeasure: «eWXR_dtJc» (record first, land second: accept never needs --no-land) removes the trap; until it lands, the env line should read "run accept WITH --no-land" rather than "adding --no-land".

## orchestrator hand-build EtAp_d8h6 (casts.md Method section + header-row test) — PR 1771
numbers: 9 tool calls end to end (pull+worktree+brief 1, inspect 3, edit+test 1, mutation+ci 1 background, fmt fix 1, commit 1, amend+push+open 1) vs. the ~30–50 a builder spends on a diff this size; wall ≈ 12 min, most of it the full gate.
- `--fix fmt FILE` is not a form (`cannot open file: fmt`); `--fix FILE` is. one wasted sub-call, the hint text on the fmt failure says `cosmic --fix <file>` and I misread it.
- committed before the gate finished (the stop hook demanded a commit); the gate then failed on fmt (2-space continuation indent inside `string.format(`), so the commit was amended — harmless on an unpushed own branch, but the order "gate, then commit" in the builder brief exists for this reason.

## orchestrator (qIC7_u6kr pulled then ended not-planned — second Evidence disproof this pass)
- `_make/imports.tl`'s `scan` is already a lexical walk that skips comments, long brackets, short strings and computed requires (its own header explains why it is a scan and not a regex); a 6-line probe file yields exactly the one real edge. The spec (mine, filed from `--find` vs. `grep` counts: 2333 real calls vs. 2632 pattern hits, 62 in comments) inferred false edges from a grep the module never runs. cost: take + worktree bootstrap (~2 min) + 3 probe calls + drop/done (1 call); saved a builder round that would have found parity and shipped a slower hot loop.
- root cause, both times (npTS, qIC7): the six "applications of the ast work" items were filed from measurements of the TREE, not readings of the MODULE each one targets. countermeasure applied: the remaining four (aAVJ, eoxA, DouF, cVq7) get their Evidence re-read against the module before any pull; countermeasure filed: «QVdk_5Ktu» (absence claims carry the search).

## builder OsLB_LlsR (doc signatures from the AST) — leg 1, STOPPED at the spec's regression case
numbers (leg 1): tool_calls=80 wall=1179s (20 min); stopped correctly: the byte-identical acceptance failed because the OLD extractor emitted 7 phantom functions from string payloads in `3p/tl/tl_patch/*.tl` and the AST reader (rightly) drops them. decision returned by SendMessage (resume, context intact): keep the fix, acceptance = exactly those removals + the new shard's own entries. cost of the stop: one orchestrator round trip (~3 calls), no rework.
agent's account (5 entries), sized:
- my env block said "copy `_build/cast_sites.tl`'s `function_name`/`owner_text`" — PR 1770 merged 4 minutes AFTER this worktree was cut, so the branch base had none of it: ~5 calls, ~10 min dead search. countermeasure: an env block never cites an in-flight PR's code as present; cite it as "on PR #N's branch, if not on your base" — standing rule from now.
- the AST-shape probe the env block mandated caught `rets` being a wrapped tuple node (`rets.typename == "tuple"`, `#rets.tuple`), not a list: ~6 probe scripts, 10 min, zero wasted design. this is the pattern that works; keep mandating it.
- inline record-type field syntax is not Teal: 2 `--check types` round trips. a `guide.gotchas` candidate if it recurs.
- old extractor's two separate full-file scans (locals, then non-locals) — order subtlety only the byte-diff acceptance would catch; no cost, noted as the acceptance earning its keep.
- "copy the index BEFORE the first edit" was lost behind the mandated probe step: `git stash -u` round trip (~3 calls) — and a bare stash on a shared stack, which the harness forbids; the resume message says never stash. countermeasure: the env block orders its before-edit steps as a numbered list, not prose.

## reviewer EtAp_d8h6 (PR 1771, casts.md doc) — FIRST tool-generated script-shaped review (pin 2026-09-06-edeebf5)
numbers: tool_calls=14 wall=102s tokens≈77k; accept --no-land first try; errors 0. agent's account: none (2 calls of help-page due diligence).
comparison: the two fresh-worktree reviews earlier this pass cost 36 and 46 calls / 297–311 s; the script shape from the tool costs 14 / 102 s — the same ratio the hand-written script prompts hit (5–11 calls). the template's own text needed two sed fixes before spawning (root path and the "own checkout" sentence: «Prnk_2udk»), and the guard step needed a per-diff instruction since the test reads the tree, not a copy — the "checkout-free mutation" is not always a file copy; the template should offer the "run the head's test against the OLD tree and watch it refuse" move as the second shape.

## reviewer eoxA_GRW9 (PR 1772, AGENTS.md reads: paragraph) — tool-generated script shape
numbers: tool_calls=17 wall=126s tokens≈80k; accept --no-land first try. agent's account, sized:
- the template says CI "is read from `gitboard show ID`" but `show` prints no CI field in the pinned build — 3 calls hunting for it (2nd reviewer this pass to trip on it; the 1770 reviewer noted `absent:` lines instead). countermeasure: the review template's CI sentence must match what `show` prints; a `show` CI line is the existing show-ci item — bump its rank.
- tried `gh pr view` (not installed): 1 call. countermeasure: the env block now says "`gh` is not installed" (added to the 1773 prompt).

## reviewer o88x_NWL9 (PR 1773, gitboard pin bump) — tool-generated script shape
numbers: tool_calls=8 wall=192s tokens≈73k; accept --no-land first try; the sha-flip mutation refused as designed ("pinned gitboard checksum verification failed"). agent's account: my claim label was `review-pin4-f6f470de` while the brief (tool-filled) said `review-o88x_NWL9-f6f470de` — no call lost because `show` disambiguated, but a wrong string would have dropped the verdict. countermeasure: always claim under the handle label the tool prints; never an ad-hoc one (my slip). also: the reviewer deleted `/home/user/cosmic/o/bootstrap/` as "transient" — it is the trust root's download cache; next `bin/gitboard` re-downloads 9.6 MB once. the script template should say what under `o/` is the tool's, not the reviewer's.

## orchestrator (gates outcome children were unranked)
- «DouF_FlP8»/«cVq7_eIpt» — the user's stated priority, outcome 3 of 26 — were `rank: unranked` since I filed them, so `next` never offered them across ~8 calls this pass; found only by reading `show` on the outcome. countermeasure: `new --parent` under a container should rank the child last by default (or `new` should print "unranked — next will not offer it"); filed below as an item. ranked both by hand now.

## builder OsLB_LlsR — full run (both legs)
numbers: tool_calls=143 (Bash 114, Edit 13, Read 14, Write 2) wall=1945s (32 min) cache_read=28.7M cache_create=451k out=1.4k; first edit at call 53. one orchestrator round trip (the STOP at the regression case → decision by SendMessage) — the resume kept its context, so leg 2 cost 63 calls with no re-reading.
leg-2 account: `--reconcile` refused the new site and took the hand row on the second run (the tool working as designed); fmt failed once on a hand-indented callback (`--fix` fixed it); "HEAD is not a descendant of origin/main" advisory twice because main moved during the build — the builder correctly did not rebase.
orchestrator-side: the branch's base predates #1770, so its `cast-sites.tsv` row is in the 3-column shape and conflicts with main's 5-column file — the merge is mine (take main's tsv, `--reconcile`, add the one refused key with its class). countermeasure: the builder brief already forbids builder rebases; the orchestrator's `worktree` should cut from the freshest main — it did; the race is #1770 merging 4 minutes later. nothing to fix beyond «JOdV_J31T» (schema-change migration sentence), which would have made this branch's own row carry the new shape.

## builder KnTF_wcsP (sweep-pattern bar sentence + REVIEW step 2) — work repo
numbers: tool_calls=39 wall=232s tokens≈96k; errors 0; agent's account: none — every measured fact in the env block (line 155, four line counts, the three-template note) matched; wall dominated by the gate. this is the floor for a two-file doctrine change: ~40 calls, 4 minutes.

## orchestrator (OsLB merge + PR 1774 open)
- `take --open` run from the builder's worktree → "not a git repository: <wt>/o/board" — 2nd time this pass (the sibling probe looks for `../work` beside the CHECKOUT, and a worktree's sibling dir is `/home/user/wt`). 1 wasted call. countermeasure: `bin/gitboard` should also try the main worktree's parent (`git rev-parse --git-common-dir`'s parent) before giving up — filed as an item below.
- merge of main into a branch whose base predates a committed-file schema change: 3-column tsv row vs 5-column file; resolved by taking main's file and letting `--reconcile` name the one missing key (it did, with path/fn/n), then adding the row with the class the builder had chosen. 4 calls + one 5-minute gate. the mechanism worked; the cost is the gate.

## reviewer KnTF_wcsP (work PR 68) — full shape, fresh worktree
numbers: tool_calls=23 wall=208s tokens≈81k; accept --no-land first try; agent's account: none. a full-shape review of a 4-file doctrine diff at 23 calls — the env block's numbered judge-list and named mutation target again did the work.

## builder cVq7_eIpt (--check lint --max-lines N, D39 amended) — PR 1775
numbers: tool_calls=92 (Bash 68, Edit 21, Read 3) wall=687s (11 min) cache_read=11.7M cache_create=133k out=2.7k; first edit at call 39; errors 1 (a `find` for a file that does not exist); repeats 7.
agent's account (4 entries), sized:
- stale-snapshot `--check types` arity error after editing a sibling's signature — 1 call; the 4th agent this pass to hit it, but this one knew the AGENTS.md line and recovered in one step: the countermeasure (documenting it, #1757) works; the env block now also names it.
- the spec's second test case ("absent → 500") was already covered by an existing test — the agent read the test file before writing a duplicate. no cost; a spec that names tests should first grep for an existing one — a refiner practice, not a tool fix.
- first mutation (delete the fallback) was caught by the checker's unused-arg warning, not by the test; a second mutation was needed — ~2 calls. the builder brief's mutation sentence could say "a mutation the checker refuses proves nothing about the test; pick one that compiles".
- the "measure first" placement guidance held (lint_test.tl 453→470).

## reviewer OsLB_LlsR (PR 1774, doc signatures from AST) — full shape, two worktrees
numbers: tool_calls=45 wall=469s tokens≈127k; accept --no-land; reproduced the changed acceptance exactly (24 leaf diffs in the three groups); 2 non-blocking findings filed as items. agent's account: the template's verdict block shows plain `accept` while the env prose says `--no-land` — 3rd reviewer to note it; «eWXR_dtJc» (record first, land second) is the fix and now ranks high.

## orchestrator (PR 1775 red in CI's build lane — the cold-build rule for signatures)
- builder's warm `--make ci` green, `coldbuild_test` green, CI `build` lane red at generation 1: the pin type-checks tree modules against its own embedded snapshot; three widened `_cli` call arities failed. cost so far: 2 cold builds to reproduce (the first, with a populated o/, did NOT reproduce — the pin loads compiled `o/_cli/*.lua` unchecked; only an o/ stripped to 3p+bootstrap does), the fix (new `_cli/lint_limit.tl`, signatures restored), one more cold build + full gate: ~12 calls, ~15 min wall. countermeasure filed: coldbuild_test models the snapshot-first include path; AGENTS.md states the rule for signatures. env-block rule from now for cosmic items: "do not widen the signature of an existing function another module calls; a new value travels in a new module or stages behind a pin bump".

## builder CM0F_Tk4k (round-aware review brief) — work repo
numbers: tool_calls=72 wall=818s (14 min) tokens≈198k; errors: see line above; agent's account (3 entries): spec's "round-1 review body" vs "round number" ambiguous (~10 min of gh.tl/ghwrite.tl reading; resolved by the env block's pointer to `bounce_context`) — countermeasure: a Change that reuses an existing read names the function; a wrapped string literal broke a one-line substring assertion (1 test run); checked whether the test file existed though the env block said so (1 call). no rework.

## orchestrator (eWXR pull refused, worktree cut anyway — 3rd time this pass)
- `take … | tail -1 && worktree …`: the pipe laundered `take`'s exit 1 (REFUSED: verdicts before new work) and `worktree` bootstrapped an unclaimed item (~2 min wall, 3 calls to undo). exactly «9doI_nexi» (worktree refuses an unclaimed item) — bump its rank; and my own chain rule: never pipe a gitboard verb before `&&`. I wrote that rule down at the start of this pass and broke it again under batching pressure — the tool-side refusal is the only countermeasure that holds.

## reviewer cVq7_eIpt (PR 1775, --max-lines + cold-build fix) — full shape
numbers: tool_calls=47 wall=362s tokens≈124k; accept --no-land; the dispatcher-seam mutation the env block named found the real gap (no test through main()), filed as an item. agent's account: "against main" read as two-dot first (2 calls) — the env block now says "three-dot"; a `printf --` slip (1 call).
- `gitboard new … "--check lint --max-lines: …"` printed the help page twice with no refusal line: a TITLE beginning with `--` is parsed as a flag. 2 wasted calls. countermeasure: `new` should treat everything after `--spec-file F` (or after a literal `--`) as the title, and print a refusal naming the unknown flag instead of the help page — small item below.

## pass tally so far (2026-09-06 22:05 → 23:35 UTC segment)
landed: cosmic #1769 #1770 #1771 #1772 #1773 #1774 (+#1775 accepted, queued); work #67 #68 (+#69 under review). items done: 0Ked, G3Mu, EtAp, eoxA, o88x, OsLB, QaZ8, KnTF (8); not-planned: npTS, qIC7 (2 spec disproofs). in build: eWXR. filed: EtAp, JOdV, QVdk, wo8d, o88x, Prnk, 7Vds, Jwsz, m1mJ, cBpk, t5hA, kkDm, rs8c, rG1E (14).
review shapes this segment: full fresh-worktree 23–47 calls; tool-generated script shape 8–17 calls; hand-builds by the orchestrator 5–9 calls for doc/pin diffs.

## reviewer CM0F_Tk4k (work PR 69) — full shape
numbers: tool_calls=37 wall=277s tokens≈106k; accept --no-land; agent's account: none. an extra unassigned probe found the round-side injection blanking untested → filed. the env block's five numbered judge points were all confirmed in order — the shape holds.

## orchestrator (aAVJ respec after #1766 landed --apply, not --fix)
- `spec ID FILE --base BASE` accepted when BASE is the item ref's own sidecar (`git -C /home/user/work show refs/heads/items/<ID>:spec.md`) — the byte-exact source `show` does not print. 1 call, no refusal. this is the working form until «wo8d_gjcB» (show --raw); noted on that item's countermeasure.
- PR 1766 (5ass) landed after the human dismissed the round-1 review: total cycle 21:08 → 23:42 (2.5 h) for a 400-line verb, ~1.5 h of it blocked on a dismissal only a human could do — «2FvN_J32f» (verdict dismisses the token's own standing review) is the fix; it could not have been done by this session type either (403), so the item may need a different token or the review to be posted as COMMENT («Qa2H_5whT»), which then never blocks.

## builder eWXR_dtJc (verdict records first, lands second) — work PR 70
numbers: tool_calls=38 wall=470s tokens≈139k; errors 0; agent's account (2 entries): the spec's "writes the verdict commit … then attempts the landing" read as two commits until the env note's "success paths unchanged" ruled it out — countermeasure: a Change that reorders steps says which paths change and which do not, in one sentence; the env block's error-format placeholder did not match `api.error_of`'s real shape (no cost; the builder used the real string) — countermeasure: never invent an error format in an env block, paste one from a test.

## builder aAVJ_FXDR (name predicates + statement deletion; the sweep) — STOPPED at an unrunnable acceptance
numbers: tool_calls=88 wall=1198s (20 min) tokens≈212k; the feature landed on the branch, gated green, mutation-tested; the acceptance sweep could not run: 10/12 grep hits are under `testdata/` (excluded from `--apply` by the project model), 2/12 are string-literal fixtures. agent's account: ~5 calls to discover it AFTER building; countermeasure: an acceptance built from a grep must be re-run as the ACTUAL command at spec time (the refiner's worked proof), not as the count — this is the 3rd Evidence-vs-module disproof this pass (npTS, qIC7, now aAVJ's sweep), all specs I filed from tree measurements; the absence-claim bar sentence («QVdk_5Ktu») covers "no rule"; a sibling sentence for "a sweep spec's command was run once and its output pasted" is the fix — filed below.
- the cast-ordinal footgun: inserting a cast before its siblings in the same function shifts their (path, fn, n) keys; harmless only when they share a class. ~5 calls. countermeasure: a doc note on `_build/cast_sites.tl` (and `--reconcile` naming the shift) — filed below.
- `take --open` from a worktree, 3rd time this pass (1 wasted call each): «Jwsz_pT5P» is the fix; until then the orchestrator's chain must `cd /home/user/cosmic` first — the cwd drifts to the last worktree touched.

## reviewer eWXR_dtJc (work PR 70) — full shape
numbers: tool_calls=30 wall=203s tokens≈99k; accept --no-land; agent's account: none (noted the `absent:` lines again — «m1mJ_0yDX»).

## reviewer rZDq_58nB (PR 1776, pin bump) — script shape
numbers: tool_calls=8 wall=68s tokens≈74k; accept --no-land; agent's account: none. the pin-bump script review is now a 1-minute, 8-call fixture: two of them this pass at 8 calls each.

## reviewer aAVJ_FXDR (PR 1777) — full shape
numbers: tool_calls=37 wall=359s tokens≈116k; accept --no-land; verified the build-time Evidence amendment (12 hits = 10 testdata + 2 string-literal fixtures) and the predicate-leak probe (no leak). agent's account: the env block named a mutation and the test it would fail; the formatter absorbed that mutation and a sibling test caught it (2 calls) — countermeasure: "expect A failure in the file", not a named test, unless the orchestrator has run the mutation; the unused-warning gate needs a placeholder reference when a branch's only call is bypassed (1 call) — a line for the builder/review briefs' mutation paragraph. non-blocking finding filed as an item.

## builder DouF_FlP8 (coverage --min replaces the committed floor) — PR 1778
numbers: tool_calls=227 (Bash 150, Edit 44, Read 28, Write 5) wall=2463s (41 min) cache_read=59.3M cache_create=334k out=4.9k; first edit at call 52; errors 0; repeats 21. the largest build this pass: ~20 files across _tool/_make/docs/decisions/workflow, two commits, cold-build reproduced by the builder itself.
agent's account (5 entries), sized:
- my env block's `flags.tl` record shape kept a `baseline` field the Change deletes — ~5 min. countermeasure: an env block states the FINAL shape, never an intermediate.
- the spec said "`embed/cosmic.mk`'s ci recipe passes the numbers" but cosmic.mk has no ci target — the env block's sentence resolved it; 1 grep + 2 reads. countermeasure: the refiner greps the target once (the absence-claim rule again, in reverse: a presence claim).
- `_build/dupes_test.tl` (duplicate-body ratchet) tripped on a copied `pct` helper — one 2.5-min gate. countermeasure: the builder brief's ratchet list names dupes_test beside casts/coverage/doc_paths.
- `_make/engine_test.tl` hardcoded `_tool/coverage/baseline_test.tl` as a fixture path — one gate run. countermeasure: before deleting a file, `git grep -n '<basename>'` — a one-line step for the brief's deletion cases.
- "strip o/ to the fetched pins" has no documented recipe; `rsync` absent — a few minutes. countermeasure: name the recipe in AGENTS.md's cold-build paragraph (`for d in $(ls o | grep -vx -e 3p -e bootstrap); do rm -rf o/$d; done; bin/cosmic --make build`) — folded into «kkDm_pEOC».

## orchestrator (PR 1778 red in CI: the cold-build rule for MAKE FLAGS)
- `make: unknown flag '--min' for ci` in 20 ms: generation 1 runs the pin's embedded make engine, which parses `--make` flags before any build; a new verb flag is unusable by CI until a release carries it — a third face of the cold-build rule (checker, signatures, now flags). the builder's local gate passed because `bin/cosmic` prefers `o/bin/cosmic` (gen 2). cost: one CI cycle + a staging edit + follow-up item. countermeasure: fold "a new `--make` flag stages behind a release" into «kkDm_pEOC»'s AGENTS.md sentence, and an env-block rule: any change to what the PINNED binary must accept on the command line (a `--make` flag, a `-c` verb) is staged — the tree can define it, CI states it after the bump.

## reviewer DouF_FlP8 (PR 1778, coverage --min) — full shape, round 1
numbers: tool_calls=67 wall=554s tokens≈158k; verified the two numbers independently (20087/26263 = 76.48%; 43 files at 0%), the cold build, the amend form, every touched file; 2 findings (dead `.cosmic-coverage merge=union` block in `.gitattributes`; two orphaned `_tool/coverage/testdata/*_baseline.txt` fixtures) — both cheap, both a "grep the basename before deleting" miss (same class as the engine_test fixture the builder hit). request-changes → HTTP 422 (bot-authored PR; «Qa2H_5whT»), so NO verdict recorded and the findings live only in chat — the 4th time this pass a bounce was lost to the 422. orchestrator fixed both by hand (3 calls + a gate) and resumes the same reviewer for round 2 (context intact).
agent's account: the brief anticipated the 422 but not why nor a fallback that posts — countermeasure: the env line should say "422 = GitHub refuses REQUEST_CHANGES on the token's own PR; report, the orchestrator relays" (added to the standing env text).

## reviewer DouF_FlP8 — round 2 (resumed, context intact)
numbers: tool_calls=9 wall=71s; accept --no-land recorded. resuming the same reviewer with the fix commit named cost 9 calls against a fresh round-2 reviewer's ~25–40 — the cheapest second round this pass; the pinned gitboard's own `<ROUND_CONTEXT>` (#69) could not help here because the 422 meant no round-1 verdict was on record for it to read.

## pass close (2026-09-07 00:50 UTC)
landed this segment: cosmic #1769 #1770 #1771 #1772 #1773 #1774 #1775 #1776 #1777 (+#1778 accepted, queued); work #67 #68 #69 #70. items done: 0Ked, G3Mu, EtAp, eoxA, o88x, OsLB, QaZ8, KnTF, cVq7, CM0F, eWXR, rZDq, 5ass, aAVJ (+DouF on merge) = 15; not-planned: npTS, qIC7; outcome «ineK_wWK6» completes with DouF. filed: 20 items (see the sections above). agents run: 24 (10 builders, 14 reviews incl. 1 resume); orchestrator hand-builds: 5 (two docs, three pin bumps) + 3 merge resolutions + 2 CI fixes.
