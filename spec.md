## Change

Every board mutation commits as `cosmic-board <board@cosmic>` — hardcoded at `_work/store.tl:43` (`-c user.email=board@cosmic -c user.name=cosmic-board`, measured 2026-08-29) — so GitHub renders every board commit Unverified and repo tooling that gates on committer identity (the operator's stop hook demands noreply@anthropic.com) flags the branch on every mutation. Change the two -c values at that line to `user.email=noreply@anthropic.com` and `user.name=Claude`. Session attribution is unaffected: the acting session is carried in the commit subject grammar (take/drop/verdict lines), never in the git author. History stays as-is — the branch is append-only and published commits are never rewritten. store.tl sits at exactly the 500-line cap: this is an in-place edit of one line, zero net growth. Update any test that asserts the old identity (`grep -rn "board@cosmic\|cosmic-board" _work/` — measure; only store.tl:43 matched outside tests today).
