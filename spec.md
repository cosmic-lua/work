Imported from whilp/cosmic#1204.

## Resolution (2026-08-18) — ended, not-planned

This item's entire premise is superseded by the current flow design and its
target files no longer exist under the names this spec names.

**The premise is gone.** `skills/work/SKILL.md` (current, on `main`) states
plainly: "ALL work state lives in items on the `board` branch — never in
GitHub labels, issue comments, notes docs, or TODO comments" and "GitHub
keeps two jobs. pull requests carry fixes... issues are the INBOUND queue
only... no workflow state ever returns to labels or issue comments." The
implementer loop's claim, PR-link and bounce steps, and the reviewer's
verdict, are now board mutations (`gitboard move ID do --claim`,
`gitboard move ID check --pr N`, `gitboard verdict`) — each one commits and
pushes to the `board` branch directly. Neither `skills/work/SKILL.md` nor
`skills/work/review.md` mandates a GitHub issue/PR comment at any of those
points today. The gap this card described — "every claim, PR link, bounce
and verdict is a hand-rolled API call" — does not exist in the current
system: there is no comment-posting step left to mechanize.

**The named target files no longer exist.** Verified against the current
`board` worktree: `_work/github.tl`, `_work/board.tl`, `_work/close.tl`,
`_work/close_test.tl`, `_work/verdict.tl`, `_work/implementer.tl`, and
`_work/stats.tl` are all absent. The tree now has `_work/gh.tl`,
`_work/gitboard.tl`, `_work/gitverdict.tl`, `_work/gitverbs.tl`,
`_work/action.tl`, and no comment-posting surface at all — the module
layout this card's `## Change` section is written against predates a
rewrite of the whole `_work/` tree.

No successor slice is filed: a `comment` verb would revive a mechanism
(state carried in GitHub comments) the current design deliberately does not
use, and nothing in the present `SKILL.md`/`review.md` asks for one. If a
future need for posting to GitHub from `gitboard` arises, it should be
filed fresh against the current `_work/` layout and the current loop steps,
not adopted from this card's stale plan.

---
_Generated by [Claude Code](https://claude.ai/code)_
