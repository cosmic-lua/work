## Change

Evidence (2026-08-30): when a change relocates or deletes a
mechanism, prose asserting the old one outlives it — item 3IZaO4Vj
needed three sweep rounds to find 23 stale sites after the identity
gate was deleted, and the two-state redesign orphaned at least three
board items targeting deleted docs. The change, prose only, in
`skills/work/SKILL.md`'s spec-bar section ("## the spec bar"): one
added sentence stating that a Change which relocates or deletes a
mechanism names the sweep it ran for sites asserting the old one —
the grep and its match count — the same way measured claims already
carry their commands. Place it beside the existing "measured, not
inferred" prose so it reads as the same rule applied to removals.
Wording stays in the file's voice (lowercase, compact). Nothing else
in the file moves.

## Non-goals

No mechanical gate (a follow-up may argue for one; this is the doc
half). No edits to AGENTS.md or other skills.
