## Evidence

On macOS arm64 at cosmic f82638e56f08c8d11bb6e4d468f05f5ebde2eb64, `GITBOARD_DIR=<work-clone> sh bin/gitboard help` printed `Downloading pinned gitboard...`, then `options follow the command (gitboard <command> [options])` and exited1. It left the verified downloaded gitboard.boot.<pid> file. SHA256 b1833c18adbb791d27dc69eb1df3ece7c0d8ad555d6b207831d73aa3cfcc0fd8 exactly matches bin/gitboard.pin (2026-09-12-b9d6a96). Running `GITBOARD_DIR=<work-clone> sh <that-verified-file> help build` succeeds and serves the build doctrine.

Source observation: bin/gitboard ensure_bootstrap invokes `sh ${boot_tmp} --assimilate` for every non-ELF download and subsequently insists the file became ELF. Unlike bin/cosmic's portable loader path, it cannot reach normal dispatch on this observed host/runtime. The source suggests the assimilation phase caused the argument diagnostic; the successful direct invocation establishes the pinned program itself is usable. Do not misdescribe this as a leading --dir bug: launcher exports GITBOARD_DIR and ends `exec ${BOOTSTRAP} "$@"`.

This blocked normal skill bootstrap and cost a manual verified-binary invocation while implementing AIK2_Q8tc. No launcher/product code changed. Search `gitboard find assimilation` found Um5y_9Nkn (general dev/CI performance), ended csdS_XEQu (test capabilities), and cache/worktree preparation items5VQX_QrYT/hLnp_iwPA, but no specific cosmic/bin/gitboard launcher repair. Refinement should preserve checksum-before-execution and use a portable native-loader contract rather than assume ELF on every supported host.
