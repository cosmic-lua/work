## Change

Evidence (review of PR 1532, 2026-08-29, probed live): `item.problems()` gates DECODE — `item.decode` calls it and `store.list` decodes every item — so the new title-shape check means a multiline title FILED BY A PRE-FIX BINARY (old builds in other sessions can still write one; their validation is client-side) bricks `list`/`show` for every fixed binary until a hand edit: the reviewer confirmed an on-disk offender makes show fail and makes the `set --title` repair path itself impossible (decode refuses before the repair applies). Split the posture: WRITE-time refusal stays exactly as landed; DECODE tolerates a malformed title (sanitize for render — first line, trimmed — or carry the problem as a warning) so reads never brick and `set --title` can repair. Add a test: hand-write an item file with a multiline title into a fixture store, assert `list`/`show` still work and `set --title` repairs it. Measure whether other problems() checks share this decode-gating hazard and report (do not widen).
