- Nothing from the dead-code half: the format-4 reader, the `migrate` verb,
  the path parsers, Ready-when, `spec.revision`, `sections`, the `## Acceptance`
  passages and the two `fsck` reports are «yXSL_x14T»'s.
- No whole-board rewrite: `key: ` is absent from every tip, so no item's tree
  changes and `fsck`'s re-encode check stays silent.
- `result` is not touched.
