## Evidence

`help orchestrate`: "Disjoint or not at all ... a shared file with thin
headroom under a line cap counts as shared." Today XH1L_z1kl (a new
decision record) was skipped for the wave because zdBC_oTPx (a D20
amendment, building) also touches `docs/decisions/README.md`, the index
`_docs/derive.tl` rewrites (`skills/decide/SKILL.md:172`). The same
holds for `docs/design/cast-sites.tsv` and `_build/casts_baseline.tl`
(`_build/casts.tl --baseline`, `_build/cast_sites.tl --reconcile`),
which every casts item touches: eight pullable casts items serialize on
files nobody edits by hand, and PR #1656 had to bump a tsv line number
by one because a `require` line moved an unrelated cast.

## Change

`_work/doctrine.tl`, the orchestrate "Disjoint or not at all" bullet:
add — a file the spec names as REGENERATED by a command (the index,
the casts baseline and site table, a coverage row) is not shared for
disjointness; on a merge conflict in such a file the resolution is
"take either side, rerun the command", which the orchestrator does
when it merges the base into the head. `help build` step 3 gains the
mirror: a Change that regenerates a derived file names the command.

`_work/doctrine_test.tl`: pin both sentences.

## Non-goals

No change to the line-keyed tsv itself (that is Iejv_42Oj).
