Evidence (2026-08-30): when a change relocates or deletes a rule, the
prose asserting the old mechanism outlives it — item 3IZaO4Vj
(PtPT_MEq6) needed three sweep rounds to find 23 stale sites after the
identity gate was deleted, and the two-state redesign orphaned at least
three more items targeting deleted docs (decompose.md, review.md). The
predecessor item (gCK9IHYy) prescribed an "inventory" demand on the old
five-section ready bar, which no longer exists; this successor targets
the current bar: `skills/work/SKILL.md`'s spec-bar section gains one
sentence — a Change that relocates or deletes a mechanism names the
sweep it ran for sites asserting the old one (the grep and its match
count), the same way measured claims already carry their commands.
Prose-only; the leverage case for a mechanical gate instead is welcome
evidence for a follow-up, not this item.
