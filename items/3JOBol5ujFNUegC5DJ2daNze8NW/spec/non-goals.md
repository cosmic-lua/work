Not reverting «ihON_6fDu», whose `new`-side derivation is correctable and
landed. Not changing `briefmeasure.measure`'s row format or its
bare-header rendering, which is deliberate and tested
(`_work/briefmeasure_test.tl:76-78`). Not changing `set --touches`, which
stays the way a caller states a list explicitly.
