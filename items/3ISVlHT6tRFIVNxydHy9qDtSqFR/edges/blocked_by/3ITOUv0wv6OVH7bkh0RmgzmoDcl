No cosmic release can publish while release.yml's 'compare against the previous release' step fails on codec_base64_roundtrip_64k: scheduled runs 32699814015 (08-24), 32818853162 (08-25) and 32940138465 (08-26) all failed at that step with the 'release' job skipped, so the latest release is still 2026-08-23-d71d7f1 — which predates 1ca3f7ff and is therefore not an eligible pin target. 3ITOUv0w names the commit that moved the scenario; the fix it leads to is what re-greens the lane. The only other route to an eligible release is a workflow_dispatch carrying perf_gate: false, which publishes outward and is a human's call.