reject verdict on PR #1643 found a real unsoundness: retyping these iterators non-nilable makes their terminating direct-call read (FileIter's subtree errors, stream/Body's read-failure message) silently dishonest and uncaught by the checker; the item's spec needs a decision on direction (retype anyway with per-site casts / keep string|nil / a narrow-only for-in mechanism) before it is pullable again