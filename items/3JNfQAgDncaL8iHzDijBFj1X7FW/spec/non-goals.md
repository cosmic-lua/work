Not changing the boundary RULE itself — claim acquisition and release
staying a separate publication boundary is correct and stays. Not making
`publish` accept a mixed composition. Not adding an automatic abandon or
recovery; naming `--abandon <COMMIT>` in the refusal text is enough.
