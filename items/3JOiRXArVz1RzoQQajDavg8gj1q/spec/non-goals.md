Not changing any help text or doctrine as part of this item — this adds the
gate. The individual corrections it turns red are separate, and several are
already filed or landed.

Not validating prose claims, only command lines: "«handoff» does X" is out of
scope, `gitboard handoff ID --session S` is in scope.

Not a general documentation linter, and not checking that the flags a line
passes are the RIGHT ones for what the surrounding prose recommends — only
that the verb exists, accepts those flags, and is not deprecated.
