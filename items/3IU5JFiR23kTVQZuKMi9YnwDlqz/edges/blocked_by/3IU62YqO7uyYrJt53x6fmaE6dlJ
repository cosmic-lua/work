the item's own ## Enablement states this as a PULL-TIME GATE: `git merge-base --is-ancestor 7b9f0749 <pin tag sha>` must hold, or the pinned binary compiles a runner-mode file with no tail and its tests silently never run. The sibling 7-batch series carries this as a structural edge; here it was prose only, so the item read as unblocked and ready-bar-clean to next.