Add a focused contract test over complete rendered briefs, following the role composition in _work/brief.tl and using _work/brieftmpl_source.tl's UNITS registry for complete template membership. UNITS contains fragments as well as whole briefs: classify fragments explicitly and check their rules through the composed roles that consume them. Give each template an explicit applicability classification for generated-code mutation guidance, board-operation ownership, and gate/coverage instructions. Verify applicable rendered guidance states source mutation and named assertion failure, assigns board mutations to the caller, and runs coverage through the full gate without separately requiring that stage. Check for conflicting instructions within the complete rendered role as well as the presence of the required rule. Make a newly registered unit fail until classified; research and other roles that do not mutate code must be explicitly exempt from that rule. Start from existing _work/brieftext_verdict_test.tl and _work/brieftext_test.tl checks and consolidate overlapping assertions rather than add parallel registries or duplicate policy prose. Regenerate renderers only if a genuine guidance inconsistency is found within these three rules.
