## Change

Evidence (re-review of PR 1518, 2026-08-29): a bounce can be cured by a change OUTSIDE the PR — 1518's prose became true when the tool fix (PR 1520) merged, with the PR's head AND spec both unmoved — but the head-already-judged guard in `_work/gitverdict.tl` (measured lines 106-119) refuses to record any verdict on an unchanged head+spec pair, so the accept was unrecordable until the reviewer force-rewrote the spec to append a Rework note. Decide and implement the sanctioned path for an environment-cured re-review: either the guard admits a re-verdict when the BOARD head moved since the prior verdict (cheap, observable), or the doctrine stays "rewrite the spec to record the cure" and gitverdict's refusal text says exactly that instead of dead-ending. Either way, the refusal text must name the way forward.
