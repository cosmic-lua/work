Not a general readability or style rule for specs, and not a word blacklist:
"every changed line" with the set named right there is fine. The rule is about
a quantifier or existence claim the BUILDER has to go resolve, not about the
words themselves.

Not changing what `new` refuses outright versus what `show` reports as a
spec-bar problem; whoever takes this decides which side of that line the rule
sits on, consistently with the rules already there.
