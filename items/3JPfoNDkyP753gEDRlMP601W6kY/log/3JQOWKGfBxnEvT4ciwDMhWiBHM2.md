Current main d630dfe7: ordinary preparation has repeatedly reused source o/bootstrap/cosmic.ape in this pass. PR184/sYow_SyfM deliberately makes --fetch bypass caches, so repeated forced acquisitions do not prove a broken cache. No machine-wide digest cache exists. work/bin/cosmic requires its native bootstrap plus .pin stamp; preparation supplies raw bootstrap names without that stamp, so a subsequent launcher can redownload. Stamp-only repair would confuse raw APE and native ELF. Refined to machine-wide pristine-byte reuse plus actual work-launcher handoff, preserving forced acquisition and representation boundaries. Cosmic's newer launcher has its own raw MZ contract and remains a separately recorded adjacent gap.