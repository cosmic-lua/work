No pin-format or trust-root change, no dependency/build-output caching, no
skipped build validation, no change to forced --fetch acquisition or receipt
semantics, no cache cleanup policy, and no changes to the separate cosmic
repository's launcher in this PR. Record its equivalent launcher gap as an
adjacent finding. This item changes worktree runtime preparation and this
repository's launcher, with focused helper and test files as needed.
