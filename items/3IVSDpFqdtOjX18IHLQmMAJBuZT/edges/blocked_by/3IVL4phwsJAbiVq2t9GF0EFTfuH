without #1468's trimmed table_kinded ({map, array}), the same three resolve lines admit record and interface nominals — measured 2026-08-27: mt is Handler flips to ok=true — reversing 3ISSFrCO's wall. Both also edit the same patch entry and the same test-file tail.