Does not add new probes or assertions, and does not change any policy. This is
a move plus an export.

Does not build a general-purpose fixture library; the scope is the raw-claim-
blob pattern those three helpers implement.
