Not re-opening «sYow_SyfM», whose behaviour change is correct, reviewed and
landed — this is the documentation that did not move with it. Not changing
`--fetch`'s behaviour, and not adding a third flag to recover the old
cache-first-under-fetch path: if that capability is wanted back, it is its
own item with its own case.
