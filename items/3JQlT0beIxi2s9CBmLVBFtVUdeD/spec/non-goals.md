No network call in a claim without `--fetch-base` or `--execute`; claim's
prepare-offline contract stays. No change to what base a current branch
captures, to the handover-ancestry refusal, to rework claims, or to the
missing-upstream refusals already present. `refresh` touches no product
repository other than the board's own.
