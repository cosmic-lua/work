Claims, drafts and `log --add` keep their format-5 paths here; `new`,
`attach`, `rank`, `set`, `spec`, `depend`, `undepend`, `take`, `verdict`
and `done` are the verbs this child makes work on a format-6 board, and
each is exercised once in the test through `store.stage`/`gitgate` rather
than through the CLI.
