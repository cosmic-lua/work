Not changing what base a claim SHOULD capture when the branch is current, and
not fetching implicitly — `--fetch-base` stays opt-in. This item makes the
documented refusal real; it does not add new fetching behaviour.

Not changing the review-side range computation. `«B9cF_Ihgn»` owns that, and
the two are the same defect at two stages — a base that is captured rather
than verified. Whoever takes this should read that item and say whether the
two should be one.
