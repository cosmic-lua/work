As of a `grep -lc "&#39;\|&#34;" o/board/items/*.md` run against the board branch on 2026-08-18, 29 item spec sidecars still carry literal HTML entities (`&#39;` for `'`, `&#34;` for `"`) throughout their bodies — not just inside code fences — including 3HyArM3A3zHuRxn3VYCvKmUZ7KW, 3HyCQm05dHCVp7IqBqm9ISGI3VU, and 3I0c25z7w2pYGONQUwkDW1oUOCA (item 3I1J83Zr had the same corruption until repaired in this session); by contrast, the three items currently in `check` phase (3I0L8yuR, 3I0uVrEj, 3I5FXjke) show zero occurrences, so the defect tracks the import path rather than being spread evenly across the board. Concretely, 3I1J83Zr's ```facts block contained the line `$ git ls-files | grep -c &#39;^_tool/floor&#39;`, which `gitboard check` hands to `/bin/sh -c` verbatim: the literal `&` is the shell's background-job operator (it forks `git ls-files | grep -c` into the background and treats everything from the next `&` onward, including the `#`, as an unrelated new command whose leading `#` then comments the rest out), so the command silently runs something other than what was authored and reports whatever bogus "expected X got Y" diff falls out, rather than a true pass/fail. This looks like every item imported from a GitHub issue via one particular "Imported from whilp/cosmic#NNNN" import path failed to HTML-decode the issue body before writing it into the `.md` sidecar, while whatever produced the check-phase items' specs did decode correctly; the fix likely belongs in that import path (whatever script or code stamps "Imported from whilp/cosmic#NNNN" at `new`-time) rather than in `gitboard check` itself, and is out of scope for this session beyond filing it.
