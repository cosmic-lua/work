Not changing `overlap.headroom_lines` itself or the 500-line cap — the
measurement is correct, it just does not reach the brief. Not changing what
`show` prints for an item that does carry `touches`.
