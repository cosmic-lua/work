## Change

Every board mutation commits as `cosmic-board <board@cosmic>`, so GitHub renders every board commit Unverified and the operator's stop hook flags the branch on each mutation. Measured 2026-08-29 (`grep -rn "board@cosmic\|cosmic-board" _work/ | grep -v _test`): TWO non-test sites hardcode the identity — `_work/store.tl:43` (the shared GIT_DEFAULTS every store commit uses) and `_work/lanes.tl:174-175` (record()'s own `git -c` pair for `sync lanes:` commits, landed with PR 1521). Change both to `user.email=noreply@anthropic.com` and `user.name=Claude`. Session attribution is unaffected (the acting session lives in the commit subject grammar, never the git author). History stays as-is — the branch is append-only; published commits are never rewritten. store.tl sits at exactly the 500-line cap: in-place edit, zero net growth there. No existing test asserts the identity strings (same grep, `_test` files): add ONE new assertion (in `_work/store_test.tl` or `_work/lanes_test.tl`, wherever a commit is already made and inspectable) that a board commit's author email is `noreply@anthropic.com`, and mutation-verify it goes red when either site reverts.
