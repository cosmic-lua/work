Not a prose-style linter and not a general docs-freshness check. Two named
assertions over two named files. No change to the limit numbers, no change to
the kind strings themselves, and no attempt to gate `docs/**` or the guides.
