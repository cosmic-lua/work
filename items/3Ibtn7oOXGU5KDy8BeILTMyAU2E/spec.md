Evidence (fresh-context review of PR #1534, 2026-08-29): AGENTS.md's
CI section opens "pr.yml: four lanes on push/PR to main" — with
`merge_group` added to pr.yml's triggers (PR #1534), that trigger
description is one event short. One-sentence doc fix in AGENTS.md's CI
section naming the merge_group trigger (queued merge candidates run the
same four lanes). Note the owner-rename residue item «3Ibt3uZM» (PR
#1535) also edits AGENTS.md — land or rebase around it rather than
conflict.
