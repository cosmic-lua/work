Extend the regression coverage around _work/prosecommands_test.tl and _work/direction_test.tl with small deterministic corpora selected from actual doctrine examples and realistic Change prose, independently of what the scanners currently recognize. State expected extracted examples and hint decisions explicitly; include ignored inputs, bare verb examples, indentation after prose, ordinary slash phrases and abbreviations, real source pointers, and quoted examples. Exercise extraction and advisory decisions through their existing interfaces. Use the PR197-to-PR212 and PR199-to-PR211 escapes as seed cases, retaining their existing regressions rather than duplicating them. Add any missing cases that discriminate scanner blindness from correct exclusion. Mutation proof must include losing an expected input form or introducing an accidental suppressor, not only changing validation after extraction. Preserve aggregate live-corpus smoke checks while making the independent fixtures the evidence of intended coverage.
