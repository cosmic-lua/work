## Change

Evidence (review of PR 1530, 2026-08-29): `item.record_involvement` and `item.problems` accept any `%S+` token as a session identity — entries are deduped but never shape-validated, so a mangled argv (the 3IU6AZEx incident absorbed a literal `--session` into builders) records garbage the distance guard then trusts. Add one identity-shape check (the derived identities are uuid-or-prefixed names; measure `_work/session.tl` for the actual shapes in use, and permit the orchestrator's minted `<word>-<digits>` and `review-<id8>-<ts>` forms) applied wherever a session name enters an item field (record_involvement, claim writes, reviewer writes), refusing tokens that begin with `-` outright. Tests: a `--session`-shaped token refuses; every live identity shape passes (enumerate from the board's own log).
