Not a general naming audit of `_work/`. This is one specific collision that
has already caused one documented misdirection; a sweep is a different item.

Not changing what either the function or the module does.
