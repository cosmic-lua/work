In the builder handoff and code-review guidance served from _work/brieftext.tl and _work/brieftext_review.tl, distinguish the reported scenario, the reproduced condition, and the condition repaired. For behavioral fixes, direct the builder to reproduce from the caller's starting state before setup can repair or hide the fault, and carry that scenario into a regression. Direct the reviewer to compare the fixture setup with the reported incident. If a nearby defect is repaired but the reported scenario remains unproven, report the change as partial and have the caller retain the unresolved incident as explicit follow-up work; do not silently discharge that scope. Locate the corresponding doctrine through _work/doctrine.tl and keep it consistent with the rendered briefs. Add focused rendered-guidance coverage for both roles, including partial closure. Use work PR194 followed by PR207 as the motivating distinction: a fetched tracking-ref fixture does not model a never-fetched clone whose local and tracking refs agree while both are stale.
