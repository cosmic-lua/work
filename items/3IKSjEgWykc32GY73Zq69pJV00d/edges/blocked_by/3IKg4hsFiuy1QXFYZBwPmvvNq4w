the C parser copies the Teal reader's contract byte for byte, and that contract mis-decodes \ddd as octal today; freezing it in C either bakes in the bug or makes the sibling differential harness report the Teal side's fault as a disagreement